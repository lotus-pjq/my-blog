---
title: 威佐夫博弈
date: 2025-08-06
category: 博弈论
tags:
  - 算法
outline: deep
---

### 威佐夫博弈（Wythoff's game）：
>有两堆各若干个物品，两个人轮流从任一堆取至少一个或同时从两堆中取同样多的物品，规定每次至少取一个，多者不限，最后取光者得胜。
- “组成部分的长度”实际上是斐波那契数列，可以方便地用数学归纳法证明。
- 因为涉及到黄金分割率，会**卡精度**！！！
- ==1.开long double==
- ==2.把sqrt换成sqrtl==
```C++
#include
using namespace std;
#define double long double
#define int long long
int a,b;
signed main(){
    while(scanf(" %lld %lld",&a,&b)!=EOF){
        if(a>b) swap(a,b);
        int d=((sqrtl(5.0)+1.0)/2.0*(b-a));
        if(a==d) printf("0\n");
        else printf("1\n");
    }
    return 0;
}
```
```
```