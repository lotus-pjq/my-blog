---
title: 找所有原根模板
date: 2026-03-09
category: 数论
tags:
  - 算法
outline: deep
---

```C++
#include
using namespace std;
#define int long long
#define endl '\n'
int pnum;
vector pri;
vector mpf,phi;//min prime pri_fac
vector rt;//是否存在原根
void get_prime(int n){
  mpf.resize(n+1,0);
  phi.resize(n+1,0);phi[1]=1;
  rt.resize(n+1,0);
  for(int i=2;in) break;
      mpf[i*p]=p;
      if(mpf[i]==p){phi[p*i]=phi[i]*p;break;}
      else phi[i*p]=phi[i]*(p-1);
    }
  }
  rt[2]=rt[4]=1;
  for(int p:pri){
    for(int j=1;(1ll*j*p)>=1;a=(a*a)%c;
  }return ans;
}
vector pri_fac;
//将phi(n)的质因子存到pri_fac数组中
void pre(int x){
  pri_fac.clear();
  int num=x,tmp;
  while(num>1){
    tmp=mpf[num];
    pri_fac.push_back(tmp);
    while(num>1&&num%tmp==0) num/=tmp;
  }
}
bool check(int i,int x){
  if(ksm(i,phi[x],x)!=1) return 0;
  for(int pf:pri_fac)
    if(ksm(i,phi[x]/pf,x)==1) return false;
  return true;
}
int min_rt(int x){
  for(int i=1;i ans;
void get_rt(int mn,int x){
  int cur=1;
  for(int i=1;i>n>>d;
  ans.clear();
  //原根数量
  if(!rt[n]) cout>t;
  while(t--) solve();
  return 0;
}
```
```
```