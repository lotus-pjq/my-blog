---
title: 线性基
date: 2025-08-14
category: 数学
tags:
  - 线性基
  - 高斯消元
  - 数学/线性代数
  - 技巧/异或
  - 模板
outline: deep
---

## 板子1：高斯消元构造：#异或- #高斯消元
- 给定 *n* 个整数（数字可能重复），求在这些数中选取任意个，使得他们的异或和最大。
### 用高斯消元得到的线性基的性质：
  - 最简阶梯形
每一行的 最高有效位（MSB） 互不相同，且 严格右移。
==从上到下单调递减==
>0...01....
0...001....
0...0001...
  - **a[0]** 保存的是 **最高位** 的基向量（最高有效位最靠左）。
**a[k-1]** 保存的是 **最低位** 的基向量（最高有效位最靠右）。
> **a[0] 的最高位 ≥ a[k-1] 的最高位**，
换句话说 **a[0] ≥ a[k-1]**（按最高有效位比较）
```C++
#include
using namespace std;
#define int long long
#define endl '\n'
int a[55];//下标从0开始
int n,k;//k:基向量个数	
void Linear_base(){
k=0;
//1.把第i位是1的数换上去
//2.把其他第i位全部消成0
for(int i=62;i>=0;i--){
for(int j=k;j>i&1){swap(a[j],a[k]);break;}
if((a[k]>>i&1)==0) continue;
for(int j=0;j>i&1)) a[j]^=a[k];
k++;if(k==n) break;
}
}
signed main(){
ios::sync_with_stdio(false);
cin.tie(nullptr);
cin>>n;
for(int i=0;i>a[i];
Linear_base();
int ans=0;
for(int i=0;i
using namespace std;
#define int long long
#define endl '\n'
int a[55];//下标从0开始
int n,k;//k:基向量个数	
void Linear_base(){
k=0;	
//1.把第i位是1的数换上去
//2.把其他第i位全部消成0
for(int i=62;i>=0;i--){
for(int j=k;j>i&1){swap(a[j],a[k]);break;}
if((a[k]>>i&1)==0) continue;
for(int j=0;j>i&1)) a[j]^=a[k];
k++;if(k==n) break;
}
}
void solve(){
cin>>n;
for(int i=0;i>a[i];
Linear_base();
int q;cin>>q;//查询次数
while(q--){
int x;cin>>x;//第x小
if(k=(1ll>i&1) ans^=a[k-i-1];
cout>t;
while(t--) solve();
return 0;
}
```
## 板子2：贪心法构造：#异或

```C++
#include
using namespace std;
#define int long long
#define endl '\n'
int n,p[64];
void insert(int x){//贪心法
for(int i=63;i>=0;--i)
if(x>>i&1){
if(!p[i]){p[i]=x;break;}//不存在则加入
x^=p[i];//存在则异或
}
}
signed main(){
ios::sync_with_stdio(false);
cin.tie(nullptr);
cin>>n;
int x;
for(int i=0;i>x;insert(x);}
int ans=0;
for(int i=63;i>=0;i--) ans=max(ans,ans^p[i]);
cout
using namespace std;
#define int long long
#define endl '\n'
const int N=2e5+5;
int	a[N],b[N];
bool flip[N];
void solve(){
int n;cin>>n;
int x=0;
for(int i=1;i>a[i],x^=a[i];
for(int i=1;i>b[i];
if(x==0){
for(int i=1;i=0;bit--){
if(((cur>>bit)&1)==0) continue;
if(!bas[bit]){
bas[bit]=cur;
msk[bit]=comp;
idx[cnt++]=i;
break;
}
cur^=bas[bit];
comp^=msk[bit];//同步维护
}
if(cnt>=30) break;
}
int cur=x;
int res=0;
for(int bit=29;bit>=0;bit--){
if(((cur>>bit)&1)==0) continue;
if(!bas[bit]) continue;
cur^=bas[bit];
res^=msk[bit];
}
if(cur!=0) cout>k)&1) flip[idx[k]]=true;
for(int i=1;i>t;
while(t--) solve();
return 0;
}
```
- tip:- 亮点1:第一步的转换，变成单个集合求子集的异或值
  - 亮点2:需要记录每个基是由哪些原来的数组成的（是由哪几个已有的基和原数异或后构成，并记录每个基的原数）
-1. 题目大意
  - 给定两个长度为 的数组 和 。你需要构造一个数组 ，满足以下条件：
  - 对于每个位置 ，有 或 。
数组 的所有元素异或和为 0，即 。
如果存在这样的 ，输出数组；否则输出 -1。
- 2.性质推导- 转化思路： 假设我们初始选择的全是 数组，此时的总异或和为 。 如果我们把第 个位置从 换成 ，总异或和的变化量为 。 即：。
  - 目标转化： 我们要选择若干个位置进行翻转（从 变成 ），使得最终异或和为 0。 假设我们要翻转的下标集合为 ，则需要满足：
  - 算法模型： 问题转化为：在集合 中，是否存在一个子集，其异或和等于 （初始 的异或和）。 这是一个经典的线性基问题。
  - 方案构造： 我们不仅需要判断是否存在，还需要知道具体选择了哪些 。
  - 构造线性基时，额外维护一个信息：当前基底是由哪些 异或得到的。
在这份代码中，采用了一种巧妙的位掩码（Mask） 方式：
记录插入线性基的第 个原始向量对应的原数组下标 idx[k]。
msk[bit] 记录基底 bas[bit] 是由哪些第 个向量组合而成的（状态压缩）。
最后查询 是否能被线性基表出。如果能，根据表出路径上的 msk 异或和，确定需要翻转哪些位置。