---
title: 最大因子数
date: 2025-08-09
category: 其他
tags:
  - 算法
outline: deep
---
```C++
#include
using namespace std;
#define int long long
#define endl '\n'
const int N=100;
int primes[N],cnt,minp[N];
bool st[N]; 
void get_primes(int n){
    for(int i=2;in){
        int k=1;
        for(int i=0;in)return;
        dfs(x+1,z);
    }
}
void solve(){
  ans=s=0;
    cin>>n;
    __int128_t sum=1;
    for(int i=0;;i++){
        if(sum>n) break;
        sum=sum*primes[i];
    s++;
    }
    dfs(0,1);
    cout>t;
    while(t--){solve();}
}
```
```
```