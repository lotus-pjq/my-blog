---
title: 树链剖分训练
date: 2025-09-18
category: 图论
tags:
  - 算法
outline: deep
---

### P3384 模板题
- Code
  - ```C++
		  #include
		  using namespace std;
		  #define int long long
		  #define endl '\n'
		  const int N=2e5+5;
		  #define ls x e[N];
		  int fa[N],dep[N],siz[N],son[N];
		  int top[N],id[N],nw[N],cnt;
		  //-----
		  struct tree{
		  	int l,r,add,sum;
		  }tr[N>1;
		  	build(ls,l,mid),build(rs,mid+1,r);
		  	pushup(x);
		  }
		  //-----
		  void dfs1(int u,int fat){//处理fa,dep,siz,son
		  	fa[u]=fat,dep[u]=dep[fat]+1,siz[u]=1;
		  	for(int v:e[u]){
		  		if(v==fat) continue;
		  		dfs1(v,u);
		  		siz[u]+=siz[v];
		  		if(siz[son[u]]tr[x].r||rtr[x].r||r>1;
		  	if(lmid) update(rs,l,r,k);
		  	pushup(x);
		  }
		  //修改子树
		  void update_tree(int u,int k){ //logn
		  	update(1,id[u],id[u]+siz[u]-1,k);
		  }
		  //修改树链:将树从x到y节点路径上所有节点权值加k
		  void update_path(int u,int v,int k){ //(logn)^2
		  	while(top[u]!=top[v]){
		  		if(dep[top[u]]>n>>m>>root>>mod;
		  	for(int i=1;i>w[i];
		  	for(int i=1,x,y;i>x>>y;
		  		e[x].push_back(y);
		  		e[y].push_back(x);
		  	}
		  	dfs1(root,0); dfs2(root,root);
		  	build(1,1,n);
		  	int op,x,y,z;
		  	while(m--){
		  		cin>>op;
		  		if(op==1) cin>>x>>y>>z,update_path(x,y,z);
		  		else if(op==2) cin>>x>>y,cout>x>>y,update_tree(x,y);
		  		else if(op==4) cin>>x,cout
		  using namespace std;
		  #define int long long
		  #define endl '\n'
		  #define ls x e[N];
		  int fa[N],dep[N],siz[N],son[N];
		  int top[N],id[N],cnt;
		  struct node{
		  	int l,r,sta,sum;
		  }t[N>1;
		  			t[ls].sum=mid-t[x].l+1;
		  			t[rs].sum=t[x].r-mid;
		  			t[ls].sta=t[rs].sta=1;
		  		}
		  		t[x].sta=-1;
		  	}
		  }
		  void build(int x,int l,int r){
		  	t[x]={l,r,-1,0};
		  	if(l==r) return;
		  	int mid=l+r>>1;
		  	build(ls,l,mid);build(rs,mid+1,r);
		  }
		  void dfs1(int u,int f){//fa,dep,siz,son
		  	fa[u]=f,dep[u]=dep[f]+1,siz[u]=1;
		  	for(int v:e[u]){
		  		if(v==f) continue;
		  		dfs1(v,u);
		  		siz[u]+=siz[v];
		  		if(siz[son[u]]t[x].r||Rt[x].r||R>1;
		  	if(L>n;
		  	for(int i=1,f;i>f;
		  		e[f+1].push_back(i+1);
		  	}
		  	dfs1(1,0);dfs2(1,1);
		  	build(1,1,n);
		  	cin>>m;
		  	string op;int x;
		  	while(m--){
		  		cin>>op>>x;x+=1;
		  		if(op[0]=='i'){
		  			cout
		  using namespace std;
		  #define int long long
		  #define endl '\n'
		  #define ls x e[N];
		  struct node{
		  	int l,r,sum,mx;
		  }t[N>1;
		  	build(ls,l,mid);build(rs,mid+1,r);
		  	pushup(x);
		  }
		  void dfs1(int u,int f){
		  	fa[u]=f,dep[u]=dep[f]+1,siz[u]=1;
		  	for(int v:e[u]){
		  		if(v==f) continue;
		  		dfs1(v,u);
		  		siz[u]+=siz[v];
		  		if(siz[son[u]]t[x].r||rt[x].r||rt[x].r||pos>1;
		  	if(posmid) update(rs,pos,k);
		  	pushup(x);
		  }
		  signed main(){
		  	ios::sync_with_stdio(false);
		  	cin.tie(nullptr);
		  	cin>>n;
		  	for(int i=1,x,y;i>x>>y;
		  		e[x].push_back(y);
		  		e[y].push_back(x);
		  	}
		  	for(int i=1;i>w[i];
		  	dfs1(1,0);dfs2(1,1);
		  	build(1,1,n);
		  	string op;int x,y;
		  	cin>>q;
		  	while(q--){
		  		cin>>op>>x>>y;
		  		if(op=="QMAX") cout
		  using namespace std;
		  #define int long long
		  #define endl '\n'
		  const int N=2e5+5;
		  #define ls x e[N];
		  int fa[N],dep[N],siz[N],son[N];
		  int top[N],id[N],nw[N],cnt;
		  //-----
		  struct tree{
		  	int l,r,add,sum;
		  }tr[N>1;
		  	build(ls,l,mid),build(rs,mid+1,r);
		  	pushup(x);
		  }
		  //-----
		  void dfs1(int u,int fat){//处理fa,dep,siz,son
		  	fa[u]=fat,dep[u]=dep[fat]+1,siz[u]=1;
		  	for(int v:e[u]){
		  		if(v==fat) continue;
		  		dfs1(v,u);
		  		siz[u]+=siz[v];
		  		if(siz[son[u]]tr[x].r||rtr[x].r||r>1;
		  	if(lmid) update(rs,l,r,k);
		  	pushup(x);
		  }
		  void update_tree(int u,int k){ //logn
		  	update(1,id[u],id[u]+siz[u]-1,k);
		  }
		  signed main(){
		  	ios::sync_with_stdio(0),cin.tie(0),cout.tie(0);
		  	cin>>n>>m;
		  	for(int i=1;i>w[i];
		  	for(int i=1,x,y;i>x>>y;
		  		e[x].push_back(y);
		  		e[y].push_back(x);
		  	}
		  	dfs1(1,0); dfs2(1,1);
		  	build(1,1,n);
		  	int op,x,y,z;
		  	while(m--){
		  		cin>>op;
		  		if(op==1) cin>>x>>z,update(1,id[x],id[x],z);
		  		else if(op==2) cin>>x>>z,update_tree(x,z);
		  		else if(op==3) cin>>x,cout
		  using namespace std;
		  #define int long long
		  #define endl '\n'
		  #define rep(i,j,k) for (int i=j;i e[N];
		  struct tree{
		  	int l,r,mx;
		  }t[N>1;
		  	build(xt[x].r||rt[x].r||p>1;
		  	if(pmid) update(x>n>>m;
		  	for(int i=1;i>a[i];
		  	for(int i=1,x,y;i>x>>y;
		  		e[x].push_back(y);
		  		e[y].push_back(x);
		  	}
		  	dfs1(root,0),dfs2(root,root);
		  	build(1,1,n);
		  	int op,x,y;
		  	while(m--){
		  		cin>>op;
		  		if(op==1){
		  			cin>>x>>y;
		  			update_tree(x,y);
		  		}else{
		  			cin>>x;
		  			cout<<query_tree(x)<<endl;
		  		}
		  	}
		      return 0;
		  }
		  ```