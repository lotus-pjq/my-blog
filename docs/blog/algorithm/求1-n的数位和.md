---
title: 求1-n的数位和
date: 2025-08-23
category: 数学
tags:
  - 算法
outline: deep
---

## Way1:
```C++
int digitSum(int n){
    if(n10^i的数位和
  	vector sum(digit,0);
  	sum[1]=45;
  	for(int i=2;i<=d;i++)
  		sum[i]=sum[i-1]*10+45*pow(10,i-1);
  	int power=pow(10,d);
  	int msd=n/power;
  	return msd*sum[d] + (msd*(msd-1)/2)*power +
  		msd*(1+n%power) + digitSum(n%power);
  }
```
```
```