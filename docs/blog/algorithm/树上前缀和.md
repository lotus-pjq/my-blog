---
title: 树上前缀和
date: 2026-03-09
category: 图论
tags:
  - 算法
description: 树上前缀和相关的算法笔记和代码模板
---

- 
- # 求树上任意两点路径边权和：
	- ## U535775 树上前缀和1
		- ### 题目：
			- ## 题目描述
			  给出 n 个点的一棵树，多次询问两点之间的最短距离。
			  注意：边是双向的。
			- ## 输入格式
			  第一行为两个整数 n 和 m。n 表示点数，m 表示询问次数；
			  下来 n-1 行，每行三个整数 x ,y, k，表示点 x 和点 y 之间存在一条边长度为 k；
			  再接下来 m 行，每行两个整数 x, y，表示询问点 x 到点 y 的最短距离。
			- ## 输出格式
			  输出 m 行。对于每次询问，输出一行。
			- ### 输入 1
			  ```
			  2 2
			  1 2 100
			  1 2
			  2 1
			  ```
			- ### 输出 1
			  ```
			  100
			  100
			  ```
			- ##### 输入 2
			  
			  ```
			  3 2
			  1 2 10
			  3 1 15
			  1 2
			  3 2
			  ```
			- ##### 输出 2
			  ```
			  10
			  25
			  ```
			- #### 说明/提示
			  对于全部数据，$2\le n\le 10^4,1\le m\le 2\times 10^4,0\lt k\le 100,1\le x,y\le n。$
		- ### Code:
			- ```C++
			  #include<bits/stdc++.h>
			  using namespace std;
			  #define int long long
			  #define endl '\n'
			  const int N=2e5+5;
			  int n,m;
			  struct node{int u,v,w;};
			  vector<node> e[N];
			  int st[N][20],dep[N];
			  int pre[N];
			  void dfs(int u,int fa){
			  	dep[u]=dep[fa]+1;
			  	st[u][0]=fa;
			  	for(int i=1;i<20;i++) st[u][i]=st[st[u][i-1]][i-1];
			  	for(node edge:e[u])
			  		if(edge.v!=fa){
			  			pre[edge.v]=pre[u]+edge.w;
			  			dfs(edge.v,u);     
			  		}         
			  }
			  int lca(int a,int b){
			  	if(dep[a]<dep[b]) swap(a,b);
			  	for(int i=19;i>=0;i--)
			  		if(dep[st[a][i]]>=dep[b]) a=st[a][i];
			  	if(a==b) return a;
			  	for(int i=19;i>=0;i--)
			  		if(st[a][i]!=st[b][i])
			  		 	a=st[a][i],b=st[b][i];
			  	return st[a][0];
			  }
			  signed main(){
			  	ios::sync_with_stdio(false);
			  	cin.tie(nullptr);
			  	cin>>n>>m;
			  	for(int i=1,x,y,z;i<n;i++){
			  		cin>>x>>y>>z;
			  		e[x].push_back({x,y,z});
			  		e[y].push_back({y,x,z});
			  	}
			  	dfs(1,0);
			  	int u,v;
			  	while(m--){
			  		cin>>u>>v;
			  		cout<<pre[u]+pre[v]-2*pre[lca(u,v)]<<endl;
			  	}
			  	return 0;
			  }
			  ```
			- ### Code:
- ### Re:从零开始的近世代数复习（easy）(河南萌新联赛2025第五场 I)：
	- ### 题目：
		- 
	- ### Code
		- ```C++
		  #include<bits/stdc++.h>
		  using namespace std;
		  #define int long long
		  #define endl '\n'
		  #define INF 0x3f3f3f3f3f3f3f3f
		  const int N=2e5+5;
		  #define pb push_back
		  int n,a[N],q,k,t1,t2;
		  int pre[N];
		  int st[N][20],dep[N];
		  vector<int> e[N];
		  void dfs(int u,int fa){
		  	dep[u]=dep[fa]+1;
		  	pre[u]=pre[fa]+a[u];
		  	st[u][0]=fa;
		  	for(int i=1;i<20;i++) st[u][i]=st[st[u][i-1]][i-1];
		  	for(int v:e[u]){
		  		if(v==fa) continue;
		  		dfs(v,u);
		  	}
		  }
		  int lca(int x,int y){
		  	if(dep[x]<dep[y]) swap(x,y);
		  	for(int i=19;i>=0;i--)
		  		if(dep[st[x][i]]>=dep[y]) x=st[x][i];
		  	if(x==y) return x;
		  	for(int i=19;i>=0;i--)
		  		if(st[x][i]!=st[y][i])
		  			x=st[x][i],y=st[y][i];
		  	return st[x][0];
		  }
		  signed main(){
		  	ios::sync_with_stdio(false);
		  	cin.tie(nullptr);
		  	cin>>n;
		  	for(int i=1;i<=n;i++) cin>>a[i];
		  	for(int i=1,x,y;i<n;i++){
		  		cin>>x>>y;
		  		e[x].pb(y),e[y].pb(x);
		  	}
		  	dfs(1,0);
		  	cin>>q;
		  	while(q--){
		  		int ans=0;
		  		cin>>k;
		  		cin>>t1>>t2;
		  		cout<<pre[t1]+pre[t2]-pre[lca(t1,t2)]<<endl;
		  	}
		  	return 0;
		  }
		  ```