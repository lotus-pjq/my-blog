---
title: 牛客多校9
date: 2025-08-12
category: 数据结构
tags:
  - 算法
outline: deep
---

### A-AVL Tree
- #AVL树
## 题目：- 
## 题解：- 

## AVL Tree定义及性质：- 
## 代码

```C++
#include
using namespace std;
#define INF 0x3f3f3f3f3f3f3f3f
#define rep(i,j,k) for(int i=j;i>n;
    rep(i,1,n) cin>>l[i]>>r[i];
    int ans=INF;
    dfs(1);
    rep(i,1,M) ans=min(ans,dp[1][i]);
    cout>t;
    while(t--) solve();
}
```
## E-Permutation
- #计数 #笛卡尔树 #组合数
## 题目- 
## 题解- 

### 补充：- ## 笛卡尔树

  ### 组合数计数求和：

### 代码

```C++
#include
using namespace std;
#define int long long
#define endl '\n'
#define INF 0x3f3f3f3f3f3f3f3f
#define rep(i,j,k) for (int i=j;i=k;--i)
#define mid (l+r>>1)
const int N=1e6+5;
const int mod=998244353;
int n,a[N];
struct node{
  int mn,pos;
}t[Nmid) posr=query(xn) return 0;
  return tt[n]*intt[m]%mod*intt[n-m]%mod;
}
int ans;
int dfs(int l,int r,int s){//s:当前节点上方节点数量
    if(r>n;int i;ans=0;
    for(i=1;i>a[i];
  build(1,1,n);
    dfs(1,n,0);
    cout>t;
    while(t--){solve();}
}
```
-