---
title: 动态开点线段树
date: 2026-03-09
category: 数据结构
tags:
  - 算法
outline: deep
---

## 思想：
- 为了节省空间，我们可以不一次性建好树，而是在最初只建立一个根结点代表整个区间。当我们需要访问某个子区间时，才建立代表这个区间的子结点。这样我们不再使用 $x
		  using namespace std;
		  #define int long long
		  #define endl '\n'
		  const int N=3e5+5;
		  struct node{
		  	int ls,rs;
		  	int sum,la;
		  }tr[N*50];
		  int n,q,id,rt;
		  void pushup(int x){
		  	tr[x].sum=tr[tr[x].ls].sum+tr[tr[x].rs].sum;
		  }
		  void pushdown(int x,int l,int r){
		  	if(tr[x].la!=-1){
		  		if(!tr[x].ls) tr[tr[x].ls=++id]={0,0,0,-1};
		  		if(!tr[x].rs) tr[tr[x].rs=++id]={0,0,0,-1};
		  		int mid=l+r>>1;
		  		tr[tr[x].ls].sum=tr[x].la*(mid-l+1);
		  		tr[tr[x].ls].la=tr[x].la;
		  		tr[tr[x].rs].sum=tr[x].la*(r-mid);
		  		tr[tr[x].rs].la=tr[x].la;
		  		tr[x].la=-1;//清除标记
		  	}
		  }
		  void change(int &p,int l,int r,int x,int y,int k){
		  	if(!p) tr[p=++id]={0,0,r-l+1,-1};
		  	if(l>=x&&r>1;
		  	if(xmid) change(tr[p].rs,mid+1,r,x,y,k);
		  	pushup(p);
		  }
		  signed main(){
		  	ios::sync_with_stdio(false);
		  	cin.tie(nullptr);
		  	cin>>n>>q;
		  	while(q--){
		  		int k,x,y;cin>>x>>y>>k;
		  		change(rt,1,n,x,y,k);
		  		cout
	  using namespace std;
	  #define int long long
	  #define endl '\n'
	  const int N=2e5+5;
	  int n,m,id,rt;
	  struct node{
	      int ls,rs;
	      int l,r;
	      int sum,lz;
	  }t[N*40];
	  int a[N],pre[N];
	  void pushup(int x){
	      t[x].sum=t[t[x].ls].sum+t[t[x].rs].sum;
	  }
	  void pushdown(int x,int l,int r){
	      if(t[x].lz){
	          if(!t[x].ls) t[t[x].ls=++id]={0,0,l,mid,0,0};
	          if(!t[x].rs) t[t[x].rs=++id]={0,0,mid+1,r,0,0};
	          int mid=l+r>>1;
	          t[t[x].ls].sum+=t[x].lz*(mid-l+1);
	          t[t[x].rs].sum+=t[x].lz*(r-mid);
	          t[t[x].ls].lz+=t[x].lz;
	          t[t[x].rs].lz+=t[x].lz;
	          t[x].lz=0;
	      }
	  }
	  void change(int&x,int l,int r,int L,int R,int k){
	      if(!x) t[x=++id]={0,0,l,r,0,0};
	      if(L>1;
	      if(L=mid+1) change(t[x].rs,mid+1,r,L,R,k);
	      pushup(x); 
	  }
	  int query(int&x,int l,int r,int L,int R){
	      if(!x) t[x=++id]={0,0,l,r,0,0};
	      if(L>1;
	      pushdown(x,l,r);
	      int res=0;
	      if(L>n>>m;
	      int mx=0;
	      for(int i=1;i>a[i],mx=max(mx,a[i]);
	      for(int i=1;i>op>>x>>y;
	          if(op==1){
	              cin>>k;
	              change(rt,1,n,x,y,k);
	          }else if(op==2){
	              cout<<query(rt,1,n,x,y)+pre[y]-pre[x-1]<<endl;
	          }
	      }
	      for(int i=1;i<=id;i++) cout<<t[i].ls<<" "<<t[i].rs<<" "<<t[i].l<<" "<<t[i].r<<endl;
	      return 0;
	  }
	  ```
-