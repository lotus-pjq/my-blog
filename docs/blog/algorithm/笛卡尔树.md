---
title: 笛卡尔树
date: 2025-09-17
category: 数据结构
tags:
  - 算法
outline: deep
---

## 介绍
- **笛卡尔树是一种特定的二叉树数据结构，它具有堆的有序性，[中序遍历]可以输出原数列。**
- **从数列中构造一棵笛卡尔树可以线性时间O(n)完成，采用基于栈的算法来找到在该数列中的所有最近小数。**
- 
- ## 用栈构建笛卡尔树:- 我们考虑将元素按下标顺序依次插入到当前的笛卡尔树中。
		  那么每次我们插入的元素必然在这棵树的右链（右链：即从根节点一直往右子树走，经过的节点形成的链）的末端。
		  于是我们执行这样一个过程，从下往上比较右链节点与当前节点 $u$ 的 $w$ ，如果找到了一个右链上的节点 $x$ 满足
		  $w_x** 当前元素 
		        k**--**
		    **if** 栈非空
		        栈顶元素.右儿子 **:=** 当前元素
		    **if** k **
	  using namespace std;
	  #define int long long
	  #define endl '\n'
	  const int N=1e7+5;
	  int n,a[N];
	  int ls[N],rs[N],sta[N];
	  void build(){
	  	int top=0;
	  	for(int i=1;i0&&a[sta[pos]]>a[i]) pos--;
	  		if(pos>0) rs[sta[pos]]=i;
	  		if(pos=r) return 1;
	  //	return dfs(l,x-1,ls[x],x)&&dfs(x+1,r,rs[x],x);
	  //}
	  //dfs(1,n,root,0);
	  signed main(){
	  	ios::sync_with_stdio(false);
	  	cin.tie(nullptr);
	  	cin>>n;
	  	for(int i=1;i>a[i];
	  	build();
	  	int ans1=0,ans2=0;
	  	for(int i=1;i
		  using namespace std;
		  #define INF 0x3f3f3f3f3f3f3f3f
		  #define int long long
		  #define endl '\n'
		  const int N=2e5+5;
		  int n,k,a[N],b[N];
		  int ls[N],rs[N],sta[N];
		  int root;
		  void build(){//建笛卡尔树
		  	int top=0;
		  	for(int i=1;i0&&b[sta[pos]]>b[i]) pos--;
		  		if(pos>0) rs[sta[pos]]=i;
		  		if(pos=r) return 1;
		  	return dfs(l,x-1,ls[x],x)&&dfs(x+1,r,rs[x],x);
		  }
		  bool check(int x){
		  	for(int i=1;i>n>>k;
		  	int mn=INF;
		  	for(int i=1;i>a[i];
		  		mn=min(mn,a[i]);
		  	}
		  	if(n==1){cout tmp;
		  	for(int i=2;i fac;
		  	for(int i=1;i*ik) continue;
		  		if(check(x)) cnt++,sum+=x;
		  	}
		  	cout>t;
		  	while(t--) solve();
		  	return 0;
		  }
		  ```
- ### 总结：
  - 建树之后利用dfs自根从上往下搜，O(n)
  - 核心代码：- ```C++
			  void build(){
			  	int top=0;
			  	for(int i=1;i0&&b[sta[pos]]>b[i]) pos--;
			  		if(pos>0) rs[sta[pos]]=i;
			  		if(pos=r) return 1;
			  	return dfs(l,x-1,ls[x],x)&&dfs(x+1,r,rs[x],x);
			  }
			  bool check(int x){
			  	for(int i=1;i
		  using namespace std;
		  #define int long long
		  #define endl '\n'
		  #define rep(i,j,k) for (int i=j;i0&&a[sta[pos]]>a[i]) pos--;
		  		if(pos>0) rs[sta[pos]]=i;
		  		if(posr) return 1;
		  	int ansl=dfs(l,x-1,ls[x],x);
		  	int ansr=dfs(x+1,r,rs[x],x);
		  	int res=ansl*ansr%mod;
		  	if(l>1) res+=ansr;
		  	if(r1&&r>n;
		  	rep(i,1,n) cin>>a[i];
		  	build();
		  	cout>t;
		      while(t--) solve();
		      return 0;
		  }
		  
		  ```