---
title: 高精度快速幂
date: 2025-10-19
category: 数学
tags:
  - 算法
outline: deep
---

- 模板

```C++
const int N=505;
int a[N>=1;
  }
}
```
- 模板题- 题目- # P1045 [NOIP 2003 普及组] 麦森数
  ## 题目描述
形如 $2^{P}-1$ 的素数称为麦森数，这时 $P$ 一定也是个素数。但反过来不一定，即如果 $P$ 是个素数，$2^{P}-1$ 不一定也是素数。到 1998 年底，人们已找到了 37 个麦森数。最大的一个是 $P=3021377$，它有 909526 位。麦森数有许多重要应用，它与完全数密切相关。
任务：输入 $P(1000
using namespace std;
#define int long long
#define endl '\n'
const int N=505;
int a[N>=1;
}
}
int p;
signed main(){
ios::sync_with_stdio(false);
cin.tie(nullptr);
cin>>p;	
int res1=p*log10(2)+1;cout<<res1<<endl;
ksm(p);
res[0]--;
for(int i=0,k=499;i<10;i++){
for(int j=0;j<50;j++,k--) cout<<res[k];
cout<<endl;
}
return 0;
}
```
```