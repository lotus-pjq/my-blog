---
title: 最小表示法
date: 2025-08-11
category: 字符串
tags:
  - 算法
outline: deep
---

- #字符串 #shift操作
### 输出:字典序最小的循环位移序列/字符串
### 序列:
```C++
// 最小表示法
int GetMin(const vector& s) {
int n = s.size();
if (n == 0) return 0;
vector t=s;
t.insert(t.end(),s.begin(),s.end());
int i=0,j=1,k=0;
while(it[j+k])?i+=k+1:j+=k+1;
if(i==j) ++i;
k=0;
}
}
return min(i,j);
}
// 返回字典序最小的循环数组
vector Minvec(const vector& s) {
int n=s.size();
if(n res;
res.reserve(n);
res.insert(res.end(),s.begin()+t,s.end()); // 后半段
res.insert(res.end(),s.begin(), s.begin()+t); // 前半段

return res;
}
```
### 字符串:
```C++
#include 
using namespace std;
//最小表示法
int GetMin(const string& s) {
    int n=s.size();
    if(n==0) return 0;
    string t=s+s; // 复制一倍
    int k=0,i=0,j=1;
    while(kt[j+k])?i=i+k+1:j=j+k+1;
            if(i==j) ++i;
            k=0;
        }
    }
    return min(i,j);
}
// 返回字典序最小的循环串
string MinStr(const string& s) {
    int n=s.size();
    if(n==0) return "";
    int t=GetMin(s);
    return s.substr(t)+s.substr(0,t);
}

signed main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n;cin>>n;
    string s;cin>>s;
    cout<<MinStr(s);
    return 0;
}
```
### 模板题:
- 洛谷P13270(字符串)
- 洛谷P1368(序列)