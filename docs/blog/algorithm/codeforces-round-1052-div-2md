---
title: Codeforces Round 1052 (Div. 2)
date: 2026-03-09
category: 题解
tags:
  - 算法
description: Codeforces Round 1052 (Div. 2)相关的算法笔记和代码模板
---

## B #贪心
	- ### 题目：
		- ![image.png](../assets/image_1758617258497_0.png)
	- ### hint：
		- ![image.png](../assets/image_1758617321241_0.png)
		-
	- ### tutorial：
		- ![image.png](../assets/image_1758617346323_0.png)
		-
	- ### 代码:
		- ```C++
		  #include<bits/stdc++.h>
		  using namespace std;
		  #define rep(i,j,k) for (int i=j;i<=k;++i)
		  #define int long long
		  #define endl '\n'
		  const int N=2e5+5;
		  #define pb push_back
		  int vis[N];
		  int n,m;
		  void solve(){
		  	cin>>n>>m;
		  	vector<int> a[n];
		  	for(int i=1;i<=m;i++) vis[i]=0;
		  	rep(i,0,n-1){
		  		int num,tmp;cin>>num;a[i].pb(num);
		  		while(num--){
		  			cin>>tmp;
		  			a[i].pb(tmp);
		  			vis[tmp]++;
		  		}
		  	}
		  	bool flag=true;
		  	for(int i=1;i<=m;i++)
		  		if(vis[i]==0) flag=false;
		  	if(!flag){cout<<"NO"<<endl;return;}
		  	int cnt=0;
		  	rep(i,0,n-1){
		  		bool flag=true;
		  		rep(j,1,a[i][0]){
		  			vis[a[i][j]]--;
		  			if(vis[a[i][j]]==0) flag=false;
		  		}
		  		rep(j,1,a[i][0]){
		  			vis[a[i][j]]++;
		  		}
		  		if(flag) cnt++;
		  		if(cnt>=2){cout<<"YES"<<endl;return;}
		  	} 
		  	cout<<"NO"<<endl;
		  }
		  signed main(){
		  	ios::sync_with_stdio(false);
		  	cin.tie(nullptr);
		  	int t;cin>>t;
		  	while(t--) solve();
		  	return 0;
		  }
		  ```
- ## D1 #按位
	- 题目：
		- ![image.png](../assets/image_1758698268791_0.png)
	- Tip：
		- 考虑“或运算”可以转换到可以“与运算”上。
		  ==A&B + A|B = A+B==
	- 思路：
		- ![image.png](../assets/image_1758698393393_0.png)
	- 代码：
		- ```C++
		  #include<bits/stdc++.h>
		  using namespace std;
		  #define INF 0x3f3f3f3f3f3f3f3f
		  const int N=2e5+5;
		  int ans[N];
		  void solve(){
		  	int l,r;cin>>l>>r;
		  	for(int i=l;i<=r;i++) ans[i]=0;
		  	int pw=1,sum=0;
		  	while(pw<r) pw=pw<<1|1;
		  	set<int> s;
		  	for(int i=l;i<=r;i++) s.insert(i);
		  	for(int i=r;i>=l;i--){
		  		while(s.count(pw-i)==0) pw>>=1;
		  		ans[i]=pw-i;
		  		sum+=i|ans[i];
		  		s.erase(pw-i);
		  	}	
		  	cout<<sum<<endl;
		  	for(int i=l;i<=r;i++) cout<<ans[i]<<" ";
		  	cout<<endl;
		  }
		  signed main(){
		  	ios::sync_with_stdio(false);
		  	cin.tie(nullptr);
		  	int t;cin>>t;
		  	while(t--) solve();
		  	return 0;
		  }
		  ```
- ## D2 #按位
	- 题目：
		- ![image.png](../assets/image_1758698080960_0.png){:height 561, :width 745}
	- 思路：
		- ![image.png](../assets/image_1758699257298_0.png){:height 424, :width 460}
	- 代码：
		- ```C++
		  #include<bits/stdc++.h>
		  using namespace std;
		  const int N=2e5+5;
		  #define int long long
		  #define endl '\n'
		  int n,m,a[N];
		  int L,R;
		  void sol(int l,int r){
		  	if(l>r) return;
		  	if(l==r){a[l-L]=r;return;}
		  	int tmp=l^r,pos=30;
		  	while(pos>0&&(l>>pos&1)==(r>>pos&1)) pos--;
		  	int mid=(r>>pos)<<pos;
		  	int tr=mid,tl=mid-1;
		  	a[tl-L]=tr,a[tr-L]=tl;
		  	while(tl-1>=l&&tr+1<=r){
		  		tl-=1;tr+=1;
		  		a[tl-L]=tr,a[tr-L]=tl;
		  	}
		  	sol(l,tl-1);
		  	sol(tr+1,r);
		  }
		  void solve(){
		  	cin>>L>>R;
		  	n=R-L+1;
		  	sol(L,R);
		  	int ans=0;
		  	for(int i=0;i<n;i++) ans+=a[i]|(i+L);
		  	cout<<ans<<endl;
		  	for(int i=0;i<n;i++) cout<<a[i]<<" ";
		  	cout<<endl;
		  }
		  signed main(){
		  	ios::sync_with_stdio(false);
		  	cin.tie(nullptr);
		  	int t;cin>>t;
		  	while(t--) solve();
		  	return 0;
		  }
		  ```