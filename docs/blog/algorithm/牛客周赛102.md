---
title: 牛客周赛102
date: 2025-07-28
category: 动态规划
tags:
  - 算法
outline: deep
---

- A
- 好01串要求01交替
- 故只有两种好串01010...和101010...
- B
```C++
void solve(){
int n;
char tmp;
cin>>n;
int op=1;
int ans=0;
for(int i=1;i>tmp;
if(op==1&&tmp=='1') ans+=i,op=-1;
else if(op==-1&&tmp=='1') ans-=2*i,op=1;
}
if(ans==0) cout>n;char tmp;
rep(i,1,n) cin>>tmp,a[i]=tmp-'0';
rep(i,1,n) b[i]=a[i];
int cnt1=0,cnt2=0;
//target:010101...
for(int i=1;i1,1->0 取反等
  - Reverse:[a1,a2,...,a10]->[a10,a9,...,a1] 序列翻转等
- 观察很容易就想到，这个符号题意的好串最后只有两种情况：
  - 00...00 11...11 00...00 11...11
  - 11...11 00...00 11...11 00...00
  - 对于上述每段0或者1串的数量不要求
- E(Easy Version) F(Hard Version) #背包
- 全1的连续子串的贡献很显然为n*(n+1)/2
- 这个时候可能会脑抽进行贪心，每次取最尽量长的1串。但会发现不符合样例2啊啊啊。
- 怎么会有人看不出来这是背包，还以为是二分+贪心捏...（所以也体现出自己背包是真的一坨啊...
- 所以是背包，
“1”：容量为1，价值为1
“2“：容量为2，价值为3
“3”：容量为3，价值为6
......
- 因为要构造的是价值为k的串，考虑到单个物品的价值是容积平方倍的，故可以考虑容量的上限肯定显然比1e3小
- dp[i]为价值为i的最短01串的长度
  - 转移：for(i:物体)
for(j:价值）
dp[j]=min(dp[j],dp[j-val[i]]+i+1);
- 因为需要还原出构造的字符串，记录dp转移的上一步是哪里转移过来，需要一个last数组来记录上一步的点last[j]=i;
- 时间复杂度O(N*√N)
- ```C++
#include
using namespace std;
#define INF 0x3f3f3f3f3f3f3f3f
#define rep(i,j,k) for (int i=j;i=k;--i)
//#define mid (l+r>>1)
//#define ls x>k;
vector ans;
while(k){
ans.push_back(last[k]);
k-=val[last[k]];
}
for(int i=1;i>t;
init();
while(t--) solve();
return 0;
}
```
- G
```
```