---
title: 杭电多校3
date: 2025-07-26
category: 字符串
tags:
  - 算法
outline: deep
---

- 1007.性质不同的数字
  异或哈希
  ```C++
  mt19937_64 rg(random_device{}());
  struct node{
  	int x,tag;
  	bool operatortmp.x;}
  };
  priority_queue q;
  unordered_map vis;
  void solve(){
  	vis.clear();
  	int n,l,r;cin>>n;
  	for(int i=1;i>l>>r;
  		int ran=rg();
  		q.push({l,ran});
  		q.push({r+1,ran});
  	}
  	vis[0]=1;
  	int cur=0,ans=1;
  	while(!q.empty()){
  		int x=q.top().x,tag=q.top().tag;q.pop();
  		cur^=tag;
  		if(!q.empty()&&q.top().x==x) continue;
  		if(!vis[cur]) ans++,vis[cur]=1;
  	}
  	cout
	  using namespace std;
	  using i64 = long long;
	  using u64 = unsigned long long;
	  using u32 = unsigned;
	  using u128 = unsigned __int128;
	  using i128 = __int128;
	  constexpr int P=998244353;
	  constexpr int B=32000;
	  vector g0[B+1],g1[B];
	  vector operator%(vector a, const vector &b) {
	      int n=b.size()-1;
	      for(int i=a.size()-1;i>=n;i--)
	          if(a[i])
	              for(int j=0;j operator*(const vector &a, const vector &b) {
	      vector c(a.size()+b.size()-1);
	      for(int i=0;i>n>>k;
	      vector a(k+1);
	    	//G(x) = Σ_{n=0} f(n)x^n
	  	// 	   = 1 + Σ_{n>=1} f(n)x^n
	  	//     = 1 + Σ_{n>=1} x^n ( a1* f(n-1) + ... + akf(n-k) )
	  	//     = 1 + a1xG(x) + a2x^2G(x) (n>=2) + ... + akx^k G(x)   (n>=k)
	    	//=> G(x) = 1 + a1xG(x) + ... + akx^kG(x)
	    	//=> G(x) = 1 / ( 1 - (a1x+...a1x^k) )  [分母多项式]
	    	// A(x) = 1 - a1x - a2x^2 - ... - ak*x^k
	      for(int i=1;i>x;
	          a[k-i]=-x%P;
	      }
	      a[k]=1;
	    
	  	vector f(k);
	      f[0]=1; 
	      for (int i=1;i k-1
	    	// j : 1 -> i
	    	// f[i] = ( f[i] - f[i-j] * a[k-j] )
	      g0[0].resize(k);
	      g0[0][0]=1;
	      for (int i = 1; i  g(k);
	      g[0]=1;
	    
	    	//
	      for (int i=0;i>b;
	          auto res=g0[b%B]*g1[b/B]%a;
	          res[0]++;
	          g=g*res%a;
	      }
	      int ans=0;
	      for (int i = 0; i < k; i++) ans=(ans+i64(g[i])*f[i])%P;
	      // ans+=g[i]*f[i];
	      if(ans<0) ans+=P;
	      cout<<ans<<endl;
	      return 0;
	  }
	  ```
	-