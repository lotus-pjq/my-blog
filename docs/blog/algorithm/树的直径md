---
title: 树的直径
date: 2026-03-09
category: 图论
tags:
  - 算法
description: 树的直径相关的算法笔记和代码模板
---

## 树上任意两节点之间最长的简单路径即为树的直径。一棵树可以有多条直径，他们的长度相等。
	- ### 方法1.两次DFS：缺点：仅适用于正边权，优点：方便记录直径的路径，时间复杂度：O(n）
	- ### 方法2.树形DP：优点：也适用于负边权，缺点：不方便记录直径的路径，时间复杂度：O(n)
- ## 求直径长度和直径路径
	- ### 模板1：边权为1的树的直径长度
		- ## B4016 树的直径
			- #### 题目描述
			  给定一棵 $n$ 个结点的树，树没有边权。请求出树的直径是多少，即树上最长的不重复经过一个点的路径长度是多少。
			- #### 输入格式
			  第一行输入一个正整数 $n$，表示结点个数。
			  第二行开始，往下一共 $n-1$ 行，每一行两个正整数 $(u,v)$，表示一条边。
			- #### 输出格式
			  输出一行，表示树的直径是多少。
			- #### 输入输出样例 1
			- >5
			  1 2
			  2 4
			  4 5
			  2 3
			- ##### 输出 1
			  ```
			  3
			  ```
			- #### 说明/提示
			  数据保证，$1 \leq n \leq 10^5$。
		- ## Code:
			- ```C++
			  #include<bits/stdc++.h>
			  using namespace std;
			  #define int long long
			  #define endl '\n'
			  const int N=1e5+5;
			  int n,id,dis[N];
			  vector<int>e[N];
			  void dfs(int u,int fa){
			  	for(int v:e[u])
			  		if(v!=fa){
			  			dis[v]=dis[u]+1;//适用于正边权
			  			if(dis[v]>dis[id]) id=v;
			  			dfs(v,u);
			  		}
			  }
			  signed main(){
			  	ios::sync_with_stdio(false);
			  	cin.tie(nullptr);
			  	cin>>n;
			  	for(int i=1,u,v;i<n;i++){
			  		cin>>u>>v;
			  		e[u].push_back(v),e[v].push_back(u);
			  	}
			  	dfs(1,0);
			  	dis[id]=0, dfs(id,0);
			  	cout<<dis[id]<<endl;
			  	return 0;
			  }
			  ```
	- ### 模板2：边权为1的树的直径路径
		- ```C++
		  #include<bits/stdc++.h>
		  using namespace std;
		  #define int long long
		  #define endl '\n'
		  const int N=1e5+5;
		  int n,id,dis[N];
		  vector<int>e[N];
		  int pre[N];
		  void dfs(int u,int fa){
		    	pre[u]=fa;
		  	for(int v:e[u])
		  		if(v!=fa){
		  			dis[v]=dis[u]+1;//适用于正边权
		  			if(dis[v]>dis[id]) id=v;
		  			dfs(v,u);
		  		}
		  }
		  signed main(){
		  	ios::sync_with_stdio(false);
		  	cin.tie(nullptr);
		  	cin>>n;
		  	for(int i=1,u,v;i<n;i++){
		  		cin>>u>>v;
		  		e[u].push_back(v),e[v].push_back(u);
		  	}
		  	dfs(1,0);
		  	dis[id]=0;
		  	dfs(id,0);
		  	cout<<dis[id]<<endl;
		  	for(int i=id;i;i=pre[i]) cout<<i<<" ";
		  	return 0;
		  }
		  ```
- ## 确定直径后，对直径上节点染色来实现O(n)计算各分支长度
	- ### 模板3：正边权树的直径长度以及所有直径公共点集
		- ### P3304 [SDOI2013] 直径
			- ### 题目描述
			  小 Q 最近学习了一些图论知识。根据课本，有如下定义。树：无回路且连通的无向图，每条边都有正整数的权值来表示其长度。如果一棵树有 $N$ 个节点，可以证明其有且仅有 $N-1$ 条边。
			  路径：一棵树上，任意两个节点之间最多有一条简单路径。我们用 $\text{dis}(a,b)$ 表示点 $a$ 和点 $b$ 的路径上各边长度之和。称 $\text{dis}(a,b)$ 为 $a,b$ 两个节点间的距离。
			  直径：一棵树上，最长的路径为树的直径。树的直径可能不是唯一的。
			  现在小 Q 想知道，对于给定的一棵树，其直径的长度是多少，以及有多少条边满足所有的直径都经过该边。
			- ### 输入格式
			  第一行包含一个整数 $N$，表示节点数。
			  接下来 $N-1$ 行，每行三个整数 $a,b,c$，表示点 $a$ 和点 $b$ 之间有一条长度为 $c$ 的无向边。
			- ### 输出格式
			  共两行。第一行一个整数，表示直径的长度。第二行一个整数，表示被所有直径经过的边的数量。
			- #### 输入 1
			  ```
			  6
			  3 1 1000
			  1 4 10
			  4 2 100
			  4 5 50
			  4 6 100
			  ```
			- #### 输出 1
			  
			  ```
			  1110 
			  2
			  ```
			- ### 说明/提示
			  直径共有两条，$3$ 到 $2$ 的路径和 $3$ 到 $6$ 的路径。这两条直径都经过边 $(3,1)$ 和边 $(1, 4)$。
			  对于 $100\%$ 的测试数据：$2\le N\le 2\times 10^5$，$1\le a,b\le N$，$0\le c \le10^9$。输入的图构成一棵树。
		- ### 思路：
			- ![image.png](../assets/image_1756348859319_0.png){:height 394, :width 708}
		- ### Code：
			- ```C++
			  #include<bits/stdc++.h>
			  using namespace std;
			  #define int long long
			  #define endl '\n'
			  const int N=2e5+5;
			  int n,id,dis[N],pre[N],cnt;
			  int l,r,mxd;
			  int col[N];
			  struct node{int u,v,w;};
			  vector<node>e[N];
			  void dfs(int u,int fa){
			  	if(dis[u]>dis[id]) id=u;
			  	pre[u]=fa;
			  	for(auto [u,v,w]:e[u])
			  		if(v!=fa&&!col[v]){
			  			dis[v]=dis[u]+w;
			  			dfs(v,u);
			  		}
			  }
			  signed main(){
			  	ios::sync_with_stdio(false);
			  	cin.tie(nullptr);
			  	cin>>n;
			  	for(int i=1,u,v,w;i<n;i++){
			  		cin>>u>>v>>w;
			  		e[u].push_back({u,v,w});
			  		e[v].push_back({v,u,w});
			  	}
			  	dfs(1,0);
			  	dis[id]=0; l=id;
			  	dfs(id,0); 
			  	mxd=dis[id]; r=id;
			  	for(int i=r;i;i=pre[i]) col[i]=1;
			  	for(int i=r;i;i=pre[i]){
			  		int ldis=dis[i],rdis=mxd-dis[i];
			  		id=i,dis[id]=0;
			  		dfs(id,pre[id]);
			  		if(dis[id]==rdis) r=i;
			  		if(dis[id]==ldis){l=i;break;}
			  		//[l,r]为公共路径
			  	}
			  	for(int i=r;i!=l;i=pre[i]) cnt++;
			  	cout<<mxd<<endl<<cnt;
			  	return 0;
			  }
			  ```
	- ### P2491 [SDOI2011] 消防
		- ### 题目：
			- ![image.png](../assets/image_1756349093114_0.png){:height 470, :width 713}
		- ### 思路：
			- ![image.png](../assets/image_1756350300999_0.png){:height 402, :width 715}
		- ### Code:
			- ```C++
			  #include<bits/stdc++.h>
			  using namespace std;
			  #define int long long
			  #define endl '\n'
			  #define INF 0x3f3f3f3f3f3f3f3f
			  const int N=3e5+5;
			  #define pb push_back
			  int n,s,r;
			  int dis[N],pre[N],col[N],id,mxd;
			  struct node{int u,v,w;};
			  vector<node> e[N];
			  void dfs(int u,int fa){
			  	if(dis[u]>dis[id]) id=u;
			  	pre[u]=fa;
			  	for(auto [u,v,w]:e[u]){
			  		if(v==fa||col[v]) continue;
			  		dis[v]=dis[u]+w;
			  		dfs(v,u);
			  	}
			  }
			  signed main(){
			  	ios::sync_with_stdio(false);
			  	cin.tie(nullptr);
			  	cin>>n>>s;
			  	for(int i=1,x,y,z;i<n;i++){
			  		cin>>x>>y>>z;
			  		e[x].pb({x,y,z}),e[y].pb({y,x,z});
			  	}
			  	dfs(1,0);
			  	dis[id]=0; dfs(id,0);
			  	r=id;
			  	int ans=INF;
			  	for(int i=r,j=r;i;i=pre[i]){
			  		while(dis[j]-dis[i]>s) j=pre[j];
			  		ans=min(ans,max(dis[i],dis[r]-dis[j]));
			  	}
			  	for(int i=r;i;i=pre[i]) col[i]=1;
			  	for(int i=r;i;i=pre[i]){
			  		id=i;dis[i]=0;
			  		dfs(i,pre[i]);
			  	}
			  	for(int i=1;i<=n;i++) ans=max(ans,dis[i]);
			  	cout<<ans;
			  	return 0;
			  }
			  ```
	- ### P3629
		- ### 题目:
			- ![image.png](../assets/image_1756350634339_0.png){:height 396, :width 593}
		- ### 思路：
			- ![image.png](../assets/image_1756350769951_0.png){:height 352, :width 597}
			-