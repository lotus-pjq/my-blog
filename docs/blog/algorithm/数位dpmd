---
title: 数位DP
date: 2026-03-09
category: 动态规划
tags:
  - 算法
description: 数位DP相关的算法笔记和代码模板
---

### P2657 [SCOI2009] windy 数
	- 题目
		- # P2657 [SCOI2009] windy 数
		- ## 题目背景
		  **本题与 [P13085 [SCOI2009] windy 数（加强版）](https://www.luogu.com.cn/problem/P13085) 的区别在于 $\bm{a}$ 与 $\bm{b}$ 的范围。**
		  windy 定义了一种 windy 数。
		- ## 题目描述
		  不含前导零且相邻两个数字之差至少为 $2$ 的正整数被称为 windy 数。windy 想知道，在 $a$ 和 $b$ 之间，包括 $a$ 和 $b$ ，总共有多少个 windy 数？
		- ## 输入格式
		  输入只有一行两个整数，分别表示 $a$ 和 $b$。
		- ## 输出格式
		  输出一行一个整数表示答案。
		- ## 输入输出样例 1
		- ### 输入 1
		  ```
		  1 10
		  ```
		- ### 输出 1
		  ```
		  9
		  ```
		- ## 输入输出样例 2
		- ### 输入 2
		  ```
		  25 50
		  ```
		- ### 输出 2
		  ```
		  20
		  ```
		- ## 说明/提示
		- #### 数据规模与约定
		  对于全部的测试点，保证 $1 \leq a \leq b \leq 2 \times 10^9$。
	- 代码
		- ```C++
		  #include<bits/stdc++.h>
		  using namespace std;
		  #define int long long
		  #define endl '\n'
		  int a,b,s[11],dp[11][11][2][2],len,cnta,cntb;
		  int dfs(int pos,int pre,bool frt0,bool lim){
		  	if(pos<=0) return 1;
		  	if(~dp[pos][pre][frt0][lim]) return dp[pos][pre][frt0][lim];
		  	int res=0,up=lim?s[pos]:9;
		  	for(int i=0;i<=up;i++)
		  		if(frt0||abs(i-pre)>=2) res+=dfs(pos-1,i,frt0&&(i==0),lim&&(i==up));
		  	return dp[pos][pre][frt0][lim]=res;
		  }
		  void init(int x){
		  	len=0;
		  	memset(dp,-1,sizeof(dp));
		  	while(x){s[++len]=x%10;x/=10;}
		  }
		  signed main(){
		      ios::sync_with_stdio(false);
		      cin.tie(nullptr);
		  	cin>>a>>b;
		  	init(a-1);
		  	cnta=dfs(len,-1,1,1);
		  	init(b);
		  	cntb=dfs(len,-1,1,1);
		  	cout<<cntb-cnta;
		      return 0;
		  }
		  ```
- ### P8764 [蓝桥杯 2021 国 BC] 二进制问题
	- 题目
		- # P8764 [蓝桥杯 2021 国 BC] 二进制问题
		- ## 题目描述
		  小蓝最近在学习二进制。他想知道 $1$ 到 $N$ 中有多少个数满足其二进制表示中恰好有 $K$ 个 `1`。你能帮助他吗？
		- ## 输入格式
		  输入一行包含两个整数 $N$ 和 $K$。
		- ## 输出格式
		  输出一个整数表示答案。
		- ## 输入输出样例 1
		- ### 输入 1
		  ```
		  7 2
		  ```
		- ### 输出 1
		  ```
		  3
		  ```
		- ## 说明/提示
		  对于 $30 \%$ 的评测用例，$1 \leq N \leq 10^{6}, 1 \leq K \leq 10$。
		  对于 $60 \%$ 的评测用例，$1 \leq N \leq 2 \times 10^{9}, 1 \leq K \leq 30$。
		  对于所有评测用例，$1 \leq N \leq 10^{18}, 1 \leq K \leq 50$。 
		  蓝桥杯 2021 国赛 B 组 H 题（C 组 J 题）。
	- 代码
		- ```C++
		  #include<bits/stdc++.h>
		  using namespace std;
		  #define int long long
		  #define endl '\n'
		  int n,k;
		  int len,s[61],dp[61][61][2][2];
		  int dfs(int pos,int cnt,bool frt0,bool lim){
		  	if(pos<=0) return cnt==k;
		  	if(~dp[pos][cnt][frt0][lim]) return dp[pos][cnt][frt0][lim];
		  	int res=0,up=lim?s[pos]:1;
		  	for(int i=0;i<=up;i++) res+=dfs(pos-1,cnt+(i==1),frt0&&(i==0),lim&&(i==up));
		  	return dp[pos][cnt][frt0][lim]=res;
		  }
		  void init(int x){
		  	len=0;
		  	memset(dp,-1,sizeof(dp));
		  	while(x){s[++len]=x%2;x>>=1;}
		  }
		  signed main(){
		      ios::sync_with_stdio(false);
		      cin.tie(nullptr);
		  	cin>>n>>k;
		  	init(n);
		  	cout<<dfs(len,0,1,1)<<endl;
		      return 0;
		  }
		  ```
- ### P6218 [USACO06NOV] Round Numbers S
	- 题目
		- # P6218 [USACO06NOV] Round Numbers S
		- ## 题目描述
		  如果一个正整数的二进制表示中，$0$ 的数目不小于 $1$ 的数目，那么它就被称为「圆数」。
		  例如，$9$ 的二进制表示为 $1001$，其中有 $2$ 个 $0$ 与 $2$ 个 $1$。因此，$9$ 是一个「圆数」。
		  请你计算，区间 $[l,r]$ 中有多少个「圆数」。
		- ## 输入格式
		  一行，两个整数 $l,r$。
		- ## 输出格式
		  一行，一个整数，表示区间 $[l,r]$ 中「圆数」的个数。
		- ## 输入输出样例 1
		- ### 输入 1
		  ```
		  2 12
		  ```
		- ### 输出 1
		  ```
		  6
		  ```
		- ## 说明/提示
		  **【数据范围】**
		  对于 $100\%$ 的数据，$1\le l,r\le 2\times 10^9$。
		  -----------
		  **【样例说明】**
		  区间 $[2,12]$ 中共有 $6$ 个「圆数」，分别为 $2,4,8,9,10,12$。
	- 代码
		- ```C++
		  #include<bits/stdc++.h>
		  using namespace std;
		  #define int long long
		  #define endl '\n'
		  int s[32],len,dp[32][32][32][2][2];
		  void init(int x){
		  	len=0;
		  	memset(dp,-1,sizeof(dp));
		  	while(x){s[++len]=x&1,x>>=1;}
		  }
		  int dfs(int pos,int cnt1,int cnt0,bool frt0,bool lim){
		  	if(pos<=0) return cnt0>=cnt1;
		  	if(~dp[pos][cnt1][cnt0][frt0][lim]) return dp[pos][cnt1][cnt0][frt0][lim];
		  	int res=0,up=lim?s[pos]:1;
		  	for(int i=0;i<=up;i++) res+=dfs(pos-1,cnt1+(i==1),cnt0+(i==0&&!frt0),frt0&&(i==0),lim&&(i==up));
		  	return dp[pos][cnt1][cnt0][frt0][lim]=res;
		  }
		  signed main(){
		      ios::sync_with_stdio(false);
		      cin.tie(nullptr);
		  	int l,r;cin>>l>>r;
		  	int cntl,cntr;
		  	init(l-1);
		  	cntl=dfs(len,0,0,1,1);
		  	init(r);
		  	cntr=dfs(len,0,0,1,1);
		  	cout<<cntr-cntl;
		      return 0;
		  }
		  ```
- ### 25春联(3)-1001 #卢卡斯定理 #数位DP
	- 题目
		- ![image.png](../assets/image_1761221075701_0.png)
		- ![image.png](../assets/image_1761221086392_0.png)
	- 思路
		- 利用了组合数模2的性质（见卢卡斯定理）
	- 代码
		- ```C++
		  #include<bits/stdc++.h>
		  using namespace std;
		  #define int long long
		  #define endl '\n'
		  const int N=1e5+5,M=32,mod=998244353;
		  int n,a[N],l[N],s[M],jud[M],dp[M][2][2];
		  int len1,len2,len,ans;
		  int dfs(int pos,bool frt0,bool lim){
		  	if(pos<=0) return 1;
		  	if(~dp[pos][frt0][lim]) return dp[pos][frt0][lim];
		  	int up=lim?s[pos]:1,res=0;
		  	for(int i=0;i<=up;i++)
		  		if(i<=jud[pos]) res+=dfs(pos-1,frt0&&(i==0),lim&&(i==up));
		  	return dp[pos][frt0][lim]=res;
		  }
		  void init(int x,int A){
		  	memset(dp,-1,sizeof(dp));
		  	len1=0;while(x>0){s[++len1]=x&1,x>>=1;}
		  	len2=0;while(A>0){jud[++len2]=A&1,A>>=1;}
		  	ans*=dfs(len1,1,1);
		  	ans%=mod; 
		  }
		  void solve(){
		  	ans=1;
		  	cin>>n;
		  	for(int i=1;i<=n;i++) cin>>a[i];
		  	for(int i=1;i<=n;i++) cin>>l[i];
		  	for(int i=1;i<=n;i++) init(min(a[i],l[i]),a[i]);
		  	cout<<ans<<endl;
		  }
		  signed main(){
		  	ios::sync_with_stdio(false);
		  	cin.tie(nullptr);
		  	int t;cin>>t;
		  	while(t--) solve();
		  	return 0;
		  }
		  ```