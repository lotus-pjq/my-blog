---
title: 背包专题
date: 2025-09-28
category: 动态规划
tags:
  - 算法
outline: deep
---

### 01背包
- 题目(P2871)- # P2871 [USACO07DEC] Charm Bracelet S
  ## 题目描述

有 $N$ 件物品和一个容量为 $M$ 的背包。第 $i$ 件物品的重量是 $W_i$，价值是 $D_i$。求解将哪些物品装入背包可使这些物品的重量总和不超过背包容量，且价值总和最大。
  ## 输入格式

第一行：物品个数 $N$ 和背包大小 $M$。

第二行至第 $N+1$ 行：第 $i$ 个物品的重量 $W_i$ 和价值 $D_i$。
  ## 输出格式

输出一行最大价值。
  ## 输入输出样例 1
  ### 输入 1

```
4 6
1 4
2 6
3 12
2 7
```
  ### 输出 1

```
23
```
  ## 说明/提示

$1 \le N \le 3402$，$1 \le M \le 12880$，$1 \le W_i \le 400$，$1 \le D \le 100$。
- 思路:
  > f[i][j]:前i件物品放入容量为j的背包的最大价值
f[i][j]=max(f[i-1][j],f[i-1][j-w[i]]+c[i])
- 未优化代码(MLE)

```C++
		  #include
		  using namespace std;
		  #define endl '\n'
		  const int N=3405;
		  int n,m;
		  int w[N],c[N];
		  int dp[N][13000];
		  //f[i][j]:前i件物品放入容量为j的背包的最大价值
		  //f[i][j]=max(f[i-1][j],f[i-1][j-w[i]]+c[i])
		  signed main(){
		  	ios::sync_with_stdio(false);
		  	cin.tie(nullptr);
		  	cin>>n>>m;
		  	for(int i=1;i>w[i]>>c[i];
		  	for(int i=1;i
		  using namespace std;
		  #define endl '\n'
		  const int N=3405;
		  int n,m;
		  int w[N],c[N];
		  int dp[2][13000];
		  //f[i][j]:前i件物品放入容量为j的背包的最大价值
		  //f[i][j]=max(f[i-1][j],f[i-1][j-w[i]]+c[i])
		  signed main(){
		  	ios::sync_with_stdio(false);
		  	cin.tie(nullptr);
		  	cin>>n>>m;
		  	for(int i=1;i>w[i]>>c[i];
		  	for(int i=1;i f[i][j]:前i件物品放入容量为j的背包的最大价值
			  f[i][j]=max(f[i][j],f[i-1][j-k\*w[i]]+k\*val[i]),k f[i][j]:前i件物品放入容量为j的背包的最大价值
			  f[i][j]=max(f[i-1][j],f[i][j-w[i]]+val[i]), j>=w[i]
- 朴素思想(TLE)

```C++
#include
using namespace std;
#define endl '\n'
const int N=1e4+5;
int n,m;
int w[N],val[N];
int dp[2][N];
//f[i][j]:前i件物品放入容量为j的背包的最大价值
//f[i][j]=max(f[i][j],f[i-1][j-k*w[i]]+k*val[i]),k>m>>n;
for(int i=1;i>val[i]>>w[i];
for(int i=1;i
using namespace std;
#define endl '\n'
const int N=1e4+5;
int n,m;
int w[N],val[N];
int dp[2][N];
//f[i][j]:前i件物品放入容量为j的背包的最大价值
//f[i][j]=max(f[i-1][j],f[i][j-w[i]]+val[i])
signed main(){
ios::sync_with_stdio(false);
cin.tie(nullptr);
cin>>m>>n;
for(int i=1;i>val[i]>>w[i];
for(int i=1;i 背包容量为W，有N种物品，每种物品最多有C个

| 时间复杂度 | 二进制拆分 ($O(NW \log C)$)  |   单调队列 ($O(NW)$)   |
| 空间复杂度 | 二进制拆分 ($O(W)$)| 单调队列 ($O(W)$)|

- 题目(P1776)- ## P1776 宝物筛选
  ### 题目描述
终于，破解了千年的难题。小 FF 找到了王室的宝物室，里面堆满了无数价值连城的宝物。
这下小 FF 可发财了，嘎嘎。但是这里的宝物实在是太多了，小 FF 的采集车似乎装不下那么多宝物。看来小 FF 只能含泪舍弃其中的一部分宝物了。
小 FF 对洞穴里的宝物进行了整理，他发现每样宝物都有一件或者多件。他粗略估算了下每样宝物的价值，之后开始了宝物筛选工作：小 FF 有一个最大载重为 $W$ 的采集车，洞穴里总共有 $n$ 种宝物，每种宝物的价值为 $v_i$，重量为 $w_i$，每种宝物有 $m_i$ 件。小 FF 希望在采集车不超载的前提下，选择一些宝物装进采集车，使得它们的价值和最大。
  ### 输入格式
第一行为两个整数 $n$ 和 $W$，分别表示宝物种数和采集车的最大载重。
接下来 $n$ 行每行三个整数 $v_i,w_i,m_i$。
  ### 输出格式
输出仅一个整数，表示在采集车不超载的情况下收集的宝物的最大价值。
  ### 输入 1
```
4 20
3 9 3
5 9 1
9 4 2
8 1 3
```
### 输出 1

```
47
```
  ### 说明/提示
对于 $30\%$ 的数据，$1 \le m_i$，$\sum m_i\leq 10^4$，$0\le W\leq 10^3$。
对于 $100\%$ 的数据，$1 \le m_i$，$\sum m_i \leq 10^5$，$0\le W\leq 4\times 10^4$，$1\leq n\le 100$。
#### 二进制拆分优化- 思路:
  - 实现
- ```C++
for(int j=1;j
using namespace std;
#define endl '\n'
const int N=1e5+5;
int n,m,wei,val,s;
int v[N],w[N];
int dp[2][N];
signed main(){
ios::sync_with_stdio(false);
cin.tie(nullptr);
cin>>n>>m;
int cnt=0;
for(int i=1;i>val>>wei>>s;
for(int j=1;j=w[i]) dp[i&1][j]=max(dp[i&1][j],dp[(i-1)&1][j-w[i]]+v[i]);
}
cout
using namespace std;
#define int long long
#define endl '\n'
const int N=4e4+5;
int n,w,q[N];
int f[2][N];
signed main(){
ios::sync_with_stdio(false);
cin.tie(nullptr);
cin>>n>>w;
int val,wei,num;
for(int i=1;i>val>>wei>>num;//价值,重量,数量
for(int j=0;j=f[(i-1)&1][q[t]]+(k-q[t])/wei*val) t--;
q[++t]=k;
f[i&1][k]=f[(i-1)&1][q[h]]+(k-q[h])/wei*val;
}
}
}
cout
using namespace std;
#define int long long
#define endl '\n'
const int N=4e4+5;
int n,w,q[N]; // 背包物品数量、背包容量、单调队列
int f[2][N];  // 滚动数组DP
signed main(){
ios::sync_with_stdio(false);
cin.tie(nullptr);
cin>>n>>w;
int val,wei,num;
// 主循环处理每个物品
for(int i=1;i>val>>wei>>num;
// 按物品重量的模数进行分组处理
for(int j=0;j=f[(i-1)&1][q[t]]+(k-q[t])/wei*val) t--;
q[++t]=k; // 当前状态入队
// 状态转移
f[i&1][k]=f[(i-1)&1][q[h]]+(k-q[h])/wei*val;
}
}
}
cout
using namespace std;
#define int long long
#define endl '\n'
const int N=1010,M=10000;
int a[M*15],b[M*15],c[M*15];
int f[2][N];
int n,m,v,w,s;
//f[i][j]:前i个物品放入容量为j背包的最大价值
//01背包:f[i][j]=max(f[i-1][j],f[i-1][j-w[i]]+v[i]);
//完全背包:f[i][j]=max(f[i-1][j],f[i][j-w[i]]+v[i])
signed main(){
ios::sync_with_stdio(false);
cin.tie(nullptr);
int h1,m1,h2,m2;char ch;
cin>>h1>>ch>>m1>>h2>>ch>>m2;
m=60*(h2-h1)+m2-m1;
cin>>n;
int p=0;
for(int i=1;i>v>>w>>s;
if(s==0){//完全背包
a[++p]=v;
b[p]=w;
c[p]=0;
}else{
for(int j=1;j=a[i]) f[i&1][j]=max(f[i&1][j],f[(i-1)&1][j-a[i]]+b[i]);
}
}else{//完全
for(int j=1;j=a[i]) f[i&1][j]=max(f[i&1][j],f[i&1][j-a[i]]+b[i]);
}
}
}
cout
using namespace std;
#define int long long
#define endl '\n'
const int N=2e3+5;
int f[2][N][N];
signed main(){
ios::sync_with_stdio(false);
cin.tie(nullptr);
int n,V,M,v,m,w;
cin>>n>>V>>M;
for(int i=1;i>v>>m>>w;
for(int j=1;j=v&&k>=m) f[i&1][j][k]=max(f[(i-1)&1][j][k],f[(i-1)&1][j-v][k-m]+w);
else f[i&1][j][k]=f[(i-1)&1][j][k];
}
}
cout
using namespace std;
#define int long long
#define endl '\n'
const int N=2e3+5;
int f[2][N][N];
signed main(){
ios::sync_with_stdio(false);
cin.tie(nullptr);
int n,M,T;cin>>n>>M>>T;
for(int i=1,m,t;i>m>>t;
for(int j=1;j=m&&k>=t) f[i&1][j][k]=max(f[(i-1)&1][j][k],f[(i-1)&1][j-m][k-t]+1);
else f[i&1][j][k]=f[(i-1)&1][j][k];
}
}
cout f[i][j]:前i组物品，能放入容量为j的背包的最大价值
f[i][j]=max(f[i][j],f[i-1][j-v[i][k]]+w[i][k])
- ```C++
for(int i=1;i=v[i][k])
f[i][j]=max(f[i][j],f[i-1][j-v[i][k]]+w[i][k]);
cout
using namespace std;
#define int long long
#define endl '\n'
#define pb push_back
const int N=1e3+5;
//f[i][j]:前i组物品，能放入容量为j的背包的最大价值
//f[i][j]=max(f[i][j],f[i-1][j-v[i][k]]+w[i][k])
int f[2][N];
vector v[N];
vector w[N];
int cnt[N];
int n,m;
map mp;
signed main(){
ios::sync_with_stdio(false);
cin.tie(nullptr);
int p=0;
cin>>m>>n;
for(int i=1,a,b,c;i>a>>b>>c;
if(mp[c]==0) mp[c]=++p;
cnt[mp[c]]++;
v[mp[c]].pb(a);
w[mp[c]].pb(b);
}
for(int i=1;i=v[i][k]) f[i&1][j]=max(f[i&1][j],f[(i-1)&1][j-v[i][k]]+w[i][k]);
else f[i&1][j]=max(f[i&1][j],f[(i-1)&1][j]);
}
}
}
cout
using namespace std;
#define int long long
#define endl '\n'
#define pb push_back
const int N=3.2e4+5;
int n,W,v,p,q;
int mw[N],mv[N];//主件重量和价值
int fw[N][3],fv[N][3];
int dp[2][N];
signed main(){
ios::sync_with_stdio(false);
cin.tie(nullptr);
cin>>W>>n;
for(int i=1,v,p,q;i>v>>p>>q;
if(!q){
mw[i]=v;
mv[i]=v*p;
}else{
fw[q][0]++;
fw[q][fw[q][0]]=v;
fv[q][fw[q][0]]=v*p;
}
}
int p=0;
for(int i=1;i=mw[i]) dp[p&1][j]=max(dp[p&1][j],dp[(p-1)&1][j-mw[i]]+mv[i]);
if(fw[i][1]&&j>=mw[i]+fw[i][1]) dp[p&1][j]=max(dp[p&1][j],dp[(p-1)&1][j-mw[i]-fw[i][1]]+mv[i]+fv[i][1]);
if(fw[i][2]&&j>=mw[i]+fw[i][2]) dp[p&1][j]=max(dp[p&1][j],dp[(p-1)&1][j-mw[i]-fw[i][2]]+mv[i]+fv[i][2]);
if(fw[i][1]&&fw[i][2]&&j>=mw[i]+fw[i][1]+fw[i][2]) dp[p&1][j]=max(dp[p&1][j],dp[(p-1)&1][j-mw[i]-fw[i][1]-fw[i][2]]+mv[i]+fv[i][1]+fv[i][2]);
}
}
}
cout 设计状态：dp[u][i][j]表示节点u的前i个子节点，限重为j能得到的最大权值和（价值和）
像01背包一样压缩空间，得到：f[u][j]:以u为根的子树，选了j门课的最大学分
递推起点：f[u][1]=w[u];
f[u][j]=max(f[u][j],f[u][j-k]+f[v][k])
01背包，逆序枚举 j:m+1->1, k:1->j-1
0号点的为必选点，所以共选 m+1门
- 代码

```C++
		  #include
		  using namespace std;
		  #define int long long
		  #define endl '\n'
		  const int N=3e3+5;
		  int n,m;
		  int w[N],f[N][N],siz[N];
		  //f[u][j]:以u为根的子树，选了j门课的最大学分
		  //递推起点：f[u][1]=w[u];
		  //f[u][j]=max(f[u][j],f[u][j-k]+f[v][k])
		  //01背包，逆序枚举 j:m+1->1, k:1->j-1
		  vector e[N];
		  void dfs(int u){
		  	f[u][1]=w[u];
		  	siz[u]=1;
		  	for(int v:e[u]){
		  		dfs(v);
		  		siz[u]+=siz[v];
		  		for(int j=min(m+1,siz[u]);j>=1;j--)
		  			for(int k=1;k>n>>m;
		  	for(int i=1,k;i>k>>w[i];
		  		e[k].push_back(i);
		  	}
		  	dfs(0);
		  	cout<<f[0][m+1];
		  	return 0;
		  }
```
```
```