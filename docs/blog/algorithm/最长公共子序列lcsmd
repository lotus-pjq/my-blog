---
title: 最长公共子序列(LCS)
date: 2026-03-09
category: 动态规划
tags:
  - 算法
description: 最长公共子序列(LCS)相关的算法笔记和代码模板
---

## 给定两个字符串，给出其最长公共子序列的长度(Longest Common Sequence)。
- ## 分析：
	- ![image.png](../assets/image_1756560154119_0.png){:height 457, :width 632}
	- ![image.png](../assets/image_1756811846212_0.png){:height 419, :width 601}
	- ![image.png](../assets/image_1756811742746_0.png){:height 372, :width 532}
	- ![image.png](../assets/image_1756812102723_0.png){:height 339, :width 494}
- ## Code1:只求长度
	- ```C++
	  #include<bits/stdc++.h>
	  using namespace std;
	  const int N=1010;
	  int n,m;
	  char a[N], b[N];
	  int f[N][N];
	  int main(){
	  	cin>>n>>m>>a+1>>b+1;
	  	for(int i=1; i<=n; i++)
	  		for(int j=1; j<=m; j++){
	  			if(a[i]==b[j]) f[i][j]=f[i-1][j-1]+1;
	  			else f[i][j]=max(f[i-1][j-1],max(f[i-1][j],f[i][j-1]));
	  		}
	  	cout<<f[n][m];
	  }
	  ```
- ## Code2:同时求出(其一)最长公共子序列
	- ```C++
	  #include<bits/stdc++.h>
	  using namespace std;
	  const int N=1e3+5;
	  int n,m,p[N][N];
	  char s[N];
	  string a,b;
	  int f[N][N];
	  signed main(){
	  	cin>>n>>m>>a>>b;
	  	a=' '+a; b=' '+b;
	  	for(int i=1;i<=n;i++)
	  		for(int j=1;j<=m;j++){
	  			if(a[i]==b[j]){
	  				f[i][j]=f[i-1][j-1]+1;
	  				p[i][j]=1;//左上方
	  			}
	  			else if(f[i][j-1]>f[i-1][j]){
	  				f[i][j]=f[i][j-1];
	  				p[i][j]=2;//左边
	  			}else{
	  				f[i][j]=f[i-1][j];
	  				p[i][j]=3;//上方
	  			}			
	  		}
	  			
	  	int i=m,j=n,k=f[m][n];
	  	while(i>0&&j>0){
	  		if(p[i][j]==1){
	  			s[k--]=a[i];
	  			i--;j--;
	  		}else if(p[i][j]==2) j--;
	  		else i--;
	  	}
	  	cout<<f[n][m]<<endl;
	  	for(i=1;i<=f[m][n];i++) cout<<s[i];
	  }
	  ```