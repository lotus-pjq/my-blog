---
title: 线段树进阶训练
date: 2025-09-15
category: 数据结构
tags:
  - 算法
outline: deep
---

## 感悟：- ### 1.用线段树解决问题，首先得考虑维护哪些信息。若不带修，任何 **满足结合律且封闭** 的信息（称为半群）都是可维护的。结合律一般都有，封闭性帮助我们设计信息。
### 2.对于lazy tag的理解
  ### lazy tag 的使用
其实这道题（维护历史最值）完全可以作为线段树的模板题，考察了 `lazy tag` 与树上节点的更新顺序的关系
  ### 如何理解？
`lazy tag` 实际上可以看作是对于该节点表示的区间的**操作序列**，这也是线段树的精髓所在
`push_down` 操作就是把父节点的操作序列接在儿子节点的序列**之后**
（*思考：为什么一定是 “ **之后** ” ？*）
对一个点进行 `push_down` 操作后，该点的操作序列即被清空，因为在递归完子树后，该点答案将被更新。以下文章中的 “ 操作序列 ”，都指的是该点上一次进行 `push_down` 后（即上一次清空后）的操作序列
  ### 如何处理操作序列？
首先，相邻的同种操作可被合并
于是，对于一个点，操作序列分为以下几种（ "+" 号表示操作的先后顺序，汉字叙述可能影响您的阅读体验）：
  - 加操作
  - 加操作+赋值操作
  - 加操作+赋值操作+加操作
  - 加操作+赋值操作+加操作+赋值操作
  - ⋯
这个操作序列显然很难保存在每一个点上
我们发现，对于一个点，首次赋值操作之后的任何修改都可以看作赋值操作（*想一想，为什么*）
于是操作序列化简为（**重点**）：
加操作 + 赋值操作
现在，恭喜您解决了两个个很大的问题：操作序列如何保存 & 同一点上操作的先后顺序问题
于是，只用保存加和赋值两种 `lazy tag`，便可记录该节点的操作序列
### 3.区间修改需要lazy，单点修改不需要lazy。
## C26 区间最大子段和- 题目- 
- 代码
```C++
#include
using namespace std;
#define int long long
#define ls x>1;
build(ls,l,mid);
build(rs,mid+1,r);
pushup(tr[x],tr[ls],tr[rs]);
}
void change(int x,int pos,int val){
if(tr[x].l==tr[x].r){tr[x]={pos,pos,val,val,val,val};return;}
int mid=tr[x].l+tr[x].r>>1;
if(pos>1;
if(r=mid+1) return query(rs,l,r);
node tmp;
pushup(tmp,query(ls,l,mid),query(rs,mid+1,r));
return tmp;
}
int n,m;
signed main(){
ios::sync_with_stdio(false);
cin>>n>>m;
for(int i=1;i>a[i];
build(1,1,n);
while(m--){
int op,x,y;cin>>op>>x>>y;
if(op==1){
if(x>y) swap(x,y);
cout
using namespace std;
#define int long long
#define endl '\n'
#define ls x>1;
build(ls,l,mid);
build(rs,mid+1,r);
pushup(tr[x],tr[ls],tr[rs]);
}
void change(int x,int pos,int val){
if(tr[x].l==tr[x].r){tr[x].sum+=val;tr[x].g+=val;return;}
int mid=tr[x].l+tr[x].r>>1;
if(pos>1;
if(rmid) return query(rs,l,r);
node tmp;
pushup(tmp,query(ls,l,r),query(rs,l,r));
return tmp;
}
signed main(){
ios::sync_with_stdio(false);
cin.tie(nullptr);
cin>>n>>m;
for(int i=1;i>a[i];
for(int i=1;i>op>>l>>r;
if(op=='C'){
cin>>d;
change(1,l,d);
if(r+1
using namespace std;
#define int long long
#define endl '\n'
#define ls x>1;
build(ls,l,mid); build(rs,mid+1,r);
}
void change(int x,int pos,int val){//点修
if(t[x].l==pos&&t[x].r==pos){t[x].mx=val;return;}
int mid=t[x].l+t[x].r>>1;
if(pos=mid+1) change(rs,pos,val);
pushup(x);
}
int query(int x,int L,int R){//区查
if(L=t[x].r) return t[x].mx;
int mid=t[x].l+t[x].r>>1;
int res=-INF;
if(Lmid) res=max(res,query(rs,L,R));
return res;
}
signed main(){
ios::sync_with_stdio(false);
cin.tie(nullptr);
cin>>m>>p;
build(1,1,m);
char op;
int x,n=0,tmp=0;
while(m--){
cin>>op>>x;
if(op=='A') change(1,++n,(x+tmp)%p);
else{
tmp=query(1,n-x+1,n);
cout
using namespace std;
#define int long long
#define endl '\n'
#define ls x>1;
build(ls,l,mid); build(rs,mid+1,r);
}
void change(int x,int pos,int k){
if(pos==t[x].l&&pos==t[x].r){t[x].mul=k;return;}
int mid=t[x].l+t[x].r>>1;
if(pos=mid+1) change(rs,pos,k);
pushup(x);
}
void solve(){
int q,op,m,pos; cin>>q>>mod;
build(1,1,q);
for(int i=1;i>op;
if(op==1) cin>>m,change(1,i,m);
else cin>>pos,change(1,pos,1);
cout>t;
while(t--) solve();
return 0;
}
```
## C30 降雨量 P2471- 题目- 
## 维护历史信息- ### 1.历史最值
  ### P4314 CPU 监控
  ### 1.题意- 给出 *T* 个数，*E* 个操作（*T*,*E*≤105）:
    - Q X Y：询问从 *X* 到 *Y* 的**当前**最大值
    - A X Y：询问从 *X* 到 *Y* 的**历史**最大值（出现过的最大数）
    - P X Y Z：将 *X* 到 *Y* 这段区间加 *Z*
    - C X Y Z：将 *X* 到 *Y* 这段区间赋值为 *Z*
### 2.历史和
### 3.矩阵乘法和历史和
## `动态开点线段树`
## 二分答案+线段树维护01区间- 题目(2016-2017 National Taiwan University World Final Team Selection Contest --- A)- 
- 题意描述
  - 给定一个长度为n的序列a, 有m次操作，每次操作选择一个区间[L,R], 将区间内的数从小到大排序，或从大到小排序，问最终序列中间的数是什么。
- 思路
  - 二分答案mid，将小于mid的数变成−1，大于mid的数变成1，等于mid的数为0，每次操作相当于询问某个区间内−1或1的个数，然后将区间重新染色，这个可以用线段树维护。最终如果中间的数是0，则二分的值就是答案，如果是−1说明答案偏大，否则答案偏小。
  - 时间复杂度: O(n\*logn\*logn)
- 代码
```C++
#include
using namespace std;
#define int long long
#define endl '\n'
#define rep(i,j,k) for (int i=j;i>1),build(rs,(l+r>>1)+1,r);
  up(x);
}
void update(int x,int l,int r,int val){
  if(t[x].l>r||t[x].r=l&&t[x].rr||t[x].r=l&&t[x].r>n>>k;
  rep(i,1,n) cin>>a[i];
  rep(i,1,k) cin>>ql[i]>>qr[i];
  int l=1,r=n,mid,ans;
  while(l>1;
    build(1,1,n);
    int cnt_neg,cnt_zero,cnt_pos;
    for(int i=1,l,r;i
using namespace std;
#define int long long
#define endl '\n'
struct cow{
  int s,e,m;
  bool operator>k>>n>>c;
  for(int i=1,x,y,z;i>x>>y>>z;
    a[i]={x,y,z};
  }
  sort(a+1,a+k+1);//按终点升序
  for(int i=1;i0){
      for(int j=a[i].s;j
using namespace std;
#define int long long
#define endl '\n'
#define mid (l+r>>1)
#define ls xt[x].r||rt[x].r||r>k>>n>>c;
  for(int i=1;i>s[i].l>>s[i].r>>s[i].m;
  sort(s+1,s+k+1);
  build(1,1,n);
  for(int i=1;i
using namespace std;
#define int long long
#define endl '\n'
#define ls x>1)
const int N=2e5+5;
int n,m,ans,c[N];
struct line{
  int l,r;
  bool operatorr) return;
  if(t[x].r=l){t[x].mn--;t[x].lazy++;return;}
  down(x);
  change(ls,l,r),change(rs,l,r);
  up(x);
}
bool query(int x,int l,int r){
  if(t[x].l>r||t[x].r=l&&t[x].r>n>>m;
  for(int i=1;i>c[i];
  for(int i=1;i>s[i].l>>s[i].r;
  sort(s+1,s+m+1);
  build(1,1,n);
  for(int i=1,l,r;i<=m;i++){
    l=s[i].l,r=s[i].r;
    if(!query(1,l,r)) continue;
    else{
      change(1,l,r);
      ans++;
    }
  }
  cout<<ans;
  return 0;
}
```