---
title: 计算几何模板整理
date: 2025-10-24
category: 计算几何
tags:
  - 算法
outline: deep
---

- 10.24整理版本
- 常量和宏
  - ```C++
		  #define double long double
		  const double eps=1e-18;
		  const double PI=acos(-1.0);
		  const int N=4e5+5;
		  const int INF=0x3f3f3f3f3f3f3f3f;
		  ```
- 点的表示

```C++
		  struct point{
		      double x,y;
		      point(double x, double y):x(x),y(y){}
		      point():x(0),y(0){}
		  }p[N];
		  point o(0,0);
		  point operator+(point a,point b){return point(a.x+b.x,a.y+b.y);}
		  point operator-(point a,point b){return point(a.x-b.x,a.y-b.y);}
		  point operator*(point a,double t){return point(a.x*t,a.y*t);}
		  point operator/(point a,double t){return point(a.x/t,a.y/t);}
		  double operator*(point a,point b){return a.x*b.y-a.y*b.x;}//叉积
		  double operator&(point a,point b){return a.x*b.x+a.y*b.y;}//点积
		  
		  double len(point a){return sqrt(a.x*a.x+a.y*a.y);}//向量长度
		  double angle(point a,point b){return acos((a&b)/len(a)/len(b));}//两向量夹角
		  double cross(point a,point b,point c){return (b.x-a.x)*(c.y-a.y)-(b.y-a.y)*(c.x-a.x);}
		  //直线ab与直线cd无交点: cross(a,b,c)*cross(a,b,d)>0
		  //直线ab与线段cd有交点: cross(a,b,c)*cross(a,b,d)0||cross(c,d,a)*cross(c,d,b)>0
		  bool intersect(point a,point b,point c,point d){//线段ab和cd是否相交
		      if(cross(a,b,c)*cross(a,b,d)>0) return 0;
		      if(cross(c,d,a)*cross(c,d,b)>0) return 0;
		      return 1;
		  }
		  double dis(point a,point b){return sqrt((a.x-b.x)*(a.x-b.x)+(a.y-b.y)*(a.y-b.y));}//两点距离
		  point norm(point a){return a/len(a);}//单位向量
		  point Joint(point a,point b,point c,point d){//求出直线ab和直线cd的交点
		  	if((d-c)*(b-a)==0) return point(INF,INF);
		  	double t=((a.y-c.y)*(c.x-d.x)-(a.x-c.x)*(c.y-d.y))/((b.x-a.x)*(c.y-d.y)-(b.y-a.y)*(c.x-d.x));
		  	return a+(b-a)*t;
		  }
		  point FootPoint(point a,point b,point c){return a+norm(b-a)*((b-a)&(c-a))/len(b-a);}//求点c在直线a,b的垂足坐标
		  point Axial_Symmetry(point a,point b,point c){return FootPoint(a,b,c)*2-c;}//求出c关于直线a,b轴对称的点c`坐标
		  bool onSegment(point p,point a,point b){return fabs((a-p)*(b-p))=da&&R>=db) return a*b/2;//case2:ab在圆内
		      point pa,pb;
		      double d=JointPoint_line_with_circle(a,b,R,pa,pb);
		      if(R=da) return a*pb/2+sector(pb,b,R);//case4,5:a在圆内
		      if(R>=db) return sector(a,pa,R)+pa*b/2;//b在圆内
		      return sector(a,pa,R)+pa*pb/2+sector(pb,b,R);//ab是割线
		  }
		  point Circle_Center(point A,point B,point C){//三角形外接圆圆心
		    	if(fabs((B-A)*(C-A)) p){//多边形顺序输入
		  	int siz=p.size();
		  	double A=0,a;
		  	p.push_back(p[0]);
		  	point g;
		  	for(int i=0;i1&&cross(s[top-1],s[top],p[i])=1;i--){//上凸包
		  		while(top>t&&cross(s[top-1],s[top],p[i])cross(s[i],s[j],s[a+1])) a=a%n+1;
		              while(cross(s[i],s[j],s[b])dot(s[i],s[i+1],s[c+1])) c=c%n+1;
		          edg=dis(s[i],s[i+1]);
		          H=cross(s[i],s[i+1],s[a])/edg;
		          L=dot(s[i+1],s[i],s[c])/edg;
		          R=dot(s[i],s[i+1],s[b])/edg;
		          len=L+R-edg;
		          if(ans>len*H){
		              ans=len*H;
		              ver[1]=s[i+1]+(s[i]-s[i+1])*(L/edg);
		              ver[2]=s[i]+(s[i+1]-s[i])*(R/edg);
		              ver[3]=ver[2]+rotate(s[i+1]-s[i],PI/2)*(H/edg);
		              ver[4]=ver[1]+rotate(s[i+1]-s[i],PI/2)*(H/edg);
		          }
		      }
		      return ans;
		  }
		  signed main(){
		      ios::sync_with_stdio(false);
		      cin.tie(nullptr);
		      cin>>n;
		      rep(i,1,n) cin>>p[i].x>>p[i].y;
		      Andrew();
		      printf("%.5lf\n",rotating_calipers());
		      int k=1;
		      for(int i=2;ieps?A
		      int k=0; double res=0;
		      for(int i=h;i>n;
		      for(int i=1;i>m;
		          for(int j=1;j>p[j].x>>p[j].y;
		          for(int j=1;j midperp(point a,point b){return {(a+b)/2,rotate(b-a,PI/2)};}//中垂线
		  circle cover(point a,point b){return{(a+b)/2,dis(a,b)/2};}//以a,b为直径的圆
		  circle cover(point a,point b,point c){//过三点的圆
		  	auto u=midperp(a,b), v=midperp(a,c);
		  	auto p=getNode(u.first,u.second,v.first,v.second);
		  	return {p,dis(p,a)};
		  }
		  ```
- 最小圆覆盖

```C++
		  void increment(int n){//最小圆覆盖（随机增量法）
		  	random_shuffle(p+1,p+n+1);
		  	cir={p[1],0};
		  	for(int i=2;i>n;
		  	for(int i=1;i>p[i].x>>p[i].y;
		  	increment(n);
		  	printf("%.10lf\n%.10lf %.10lf\n",cir.r,cir.p.x,cir.p.y);
		  }
		  ```
- 最近平面点对

```C++
		  point aa[N],bb[N];
		  double divide(int l,int r){//最近平面点对:分治+归并排序 O(NlogN)
		  	double d=2e9;
		  	if(l==r) return d;
		  	int m=l+r>>1; point tmp=aa[m];
		  	d=min(divide(l,m),divide(m+1,r));
		  	int i=l,j=m+1,k=0,t=0;
		  	while(i>n;//n>aa[i].x>>aa[i].y;
		  	auto cmp=[](point a,point b){return a.x
		  using namespace std;
		  #define int long long
		  #define endl '\n'
		  
		  #define double long double
		  const double eps=1e-18;
		  const double PI=acos(-1.0);
		  const int N=4e5+5;
		  const int INF=0x3f3f3f3f3f3f3f3f;
		  
		  struct point{
		      double x,y;
		      point(double x, double y):x(x),y(y){}
		      point():x(0),y(0){}
		  }p[N];
		  point o(0,0);
		  point operator+(point a,point b){return point(a.x+b.x,a.y+b.y);}
		  point operator-(point a,point b){return point(a.x-b.x,a.y-b.y);}
		  point operator*(point a,double t){return point(a.x*t,a.y*t);}
		  point operator/(point a,double t){return point(a.x/t,a.y/t);}
		  double operator*(point a,point b){return a.x*b.y-a.y*b.x;}//叉积
		  double operator&(point a,point b){return a.x*b.x+a.y*b.y;}//点积
		  
		  double len(point a){return sqrt(a.x*a.x+a.y*a.y);}//向量长度
		  double angle(point a,point b){return acos((a&b)/len(a)/len(b));}//两向量夹角
		  double cross(point a,point b,point c){return (b.x-a.x)*(c.y-a.y)-(b.y-a.y)*(c.x-a.x);}
		  //直线ab与直线cd无交点: cross(a,b,c)*cross(a,b,d)>0
		  //直线ab与线段cd有交点: cross(a,b,c)*cross(a,b,d)0||cross(c,d,a)*cross(c,d,b)>0
		  bool intersect(point a,point b,point c,point d){//线段ab和cd是否相交
		      if(cross(a,b,c)*cross(a,b,d)>0) return 0;
		      if(cross(c,d,a)*cross(c,d,b)>0) return 0;
		      return 1;
		  }
		  double dis(point a,point b){return sqrt((a.x-b.x)*(a.x-b.x)+(a.y-b.y)*(a.y-b.y));}//两点距离
		  point norm(point a){return a/len(a);}//单位向量
		  point Joint(point a,point b,point c,point d){//求出直线ab和直线cd的交点
		  	point u=b-a,v=d-c,w=a-c;
		  	double t=(v*w)/(u*v);
		  	if(fabs(u*v)=da&&R>=db) return a*b/2;//case2:ab在圆内
		      point pa,pb;
		      double d=JointPoint_line_with_circle(a,b,R,pa,pb);
		      if(R=da) return a*pb/2+sector(pb,b,R);//case4,5:a在圆内
		      if(R>=db) return sector(a,pa,R)+pa*b/2;//b在圆内
		      return sector(a,pa,R)+pa*pb/2+sector(pb,b,R);//ab是割线
		  }
		  point Circle_Center(point A,point B,point C){//三角形外接圆圆心
		    	if(fabs((B-A)*(C-A)) p){//多边形重心公司
		  	int siz=p.size();
		  	double A=0,a;
		  	p.push_back(p[0]);
		  	point g;
		  	for(int i=0;i p){
		  	int siz=p.size();
		  	if(siz p){//判断点u在多边形(内:1,外:0,上:-1)
		  	point v(1e9+5,2e8+5);
		  	int cnt=0,siz=p.size();
		  	p.push_back(p[0]);
		  	for(int i=0;i1&&cross(s[top-1],s[top],p[i])=1;i--){//上凸包
		  		while(top>t&&cross(s[top-1],s[top],p[i])cross(s[i],s[j],s[a+1])) a=a%n+1;
		              while(cross(s[i],s[j],s[b])eps?A
		      int k=0; double res=0;
		      for(int i=h;i>n;
		      for(int i=1;i>m;
		          for(int j=1;j>p[j].x>>p[j].y;
		          for(int j=1;j midperp(point a,point b){return {(a+b)/2,point(b.y-a.y,a.x-b.x)};}//中垂线
		  circle cover(point a,point b){return{(a+b)/2,dis(a,b)/2};}//以a,b为直径的圆
		  circle cover(point a,point b,point c){//过三点的圆
		  	auto u=midperp(a,b), v=midperp(a,c);
		  	auto p=getNode(u.first,u.second,v.first,v.second);
		  	return {p,dis(p,a)};
		  }
		  
		  //最小圆覆盖（随机增量法）
		  void increment(int n){
		  	random_shuffle(p+1,p+n+1);
		  	cir={p[1],0};
		  	for(int i=2;i>n;
		  	for(int i=1;i>p[i].x>>p[i].y;
		  	increment(n);
		  	printf("%.10lf\n%.10lf %.10lf\n",cir.r,cir.p.x,cir.p.y);
		  }
		  //-----------------------------------------------------------
		  //最近平面点对:分治+归并排序 O(NlogN)
		  point aa[N],bb[N];
		  double divide(int l,int r){
		  	double d=2e9;
		  	if(l==r) return d;
		  	int m=l+r>>1; point tmp=aa[m];
		  	d=min(divide(l,m),divide(m+1,r));
		  	int i=l,j=m+1,k=0,t=0;
		  	while(i>n;//n>aa[i].x>>aa[i].y;
		  	auto cmp=[](point a,point b){return a.x p){//多边形重心公司
		  	int siz=p.size();
		  	double A=0,a;
		  	p.push_back(p[0]);
		  	point g;
		  	for(int i=0;i p){
		  	int siz=p.size();
		  	if(siz p){//判断点u在多边形(内:1,外:0,上:-1)
		  	point v(1e9+5,2e8+5);
		  	int cnt=0,siz=p.size();
		  	p.push_back(p[0]);
		  	for(int i=0;i=0&&i=0&&j-2) turn(i-1,j,x0-z,y,x-x0,x0-H,y0,H,W,L);
		  		if(j-2) turn(i,j-1,x,y0-z,y-y0,x0,y0-H,L,H,W);
		  	}
		  }
		  //(x1,y1,z1), (x2,y2,z2): 两个点的坐标
		  //L,W,H: 长方体的长、宽、高
		  int cal(int x1,int y1,int z1,int x2,int y2,int z2,int L,int W,int H){
		  	if(z1!=0&&z1!=H)if(y1==0||y1==W)
		  		swap(y1,z1),swap(y2,z2),swap(W,H);
		  	else
		  		swap(x1,z1),swap(x2,z2),swap(L,H);
		  	if(z1==H) z1=0,z2=H-z2;
		  	r=~0U>>1;
		  	turn(0,0,x2-x1,y2-y1,z2,-x1,-y1,L,W,H);
		  	return r;
		  }
		  void Distance_between_two_points_on_the_surface_of_a_rectangular_prism(){
		  	int L=10,W=8,H=6;
		  	int dis=sqrt(cal(2,3,0, 7,5,6,L,W,H));
		  	cout p){//多边形重心公司
	  	int siz=p.size();
	  	double A=0,a;
	  	p.push_back(p[0]);
	  	point g;
	  	for(int i=0;i p){
	  	int siz=p.size();
	  	if(siz p){//判断点u在多边形(内:1,外:0,上:-1)
	  	point v(1e9+5,2e8+5);
	  	int cnt=0,siz=p.size();
	  	p.push_back(p[0]);
	  	for(int i=0;i
	  using namespace std;
	  #define int long long
	  #define endl '\n'
	  
	  #define double long double
	  const double eps=1e-18;
	  const double PI=acos(-1.0);
	  const int N=4e5+5;
	  const int INF=0x3f3f3f3f3f3f3f3f;
	  
	  struct point{
	      double x,y;
	      point(double x, double y):x(x),y(y){}
	      point():x(0),y(0){}
	  }p[N];
	  point o(0,0);
	  point operator+(point a,point b){return point(a.x+b.x,a.y+b.y);}
	  point operator-(point a,point b){return point(a.x-b.x,a.y-b.y);}
	  point operator*(point a,double t){return point(a.x*t,a.y*t);}
	  point operator/(point a,double t){return point(a.x/t,a.y/t);}
	  double operator*(point a,point b){return a.x*b.y-a.y*b.x;}//叉积
	  double operator&(point a,point b){return a.x*b.x+a.y*b.y;}//点积
	  
	  double len(point a){return sqrt(a.x*a.x+a.y*a.y);}//向量长度
	  double angle(point a,point b){return acos((a&b)/len(a)/len(b));}//两向量夹角
	  double cross(point a,point b,point c){return (b.x-a.x)*(c.y-a.y)-(b.y-a.y)*(c.x-a.x);}
	  //直线ab与直线cd无交点: cross(a,b,c)*cross(a,b,d)>0
	  //直线ab与线段cd有交点: cross(a,b,c)*cross(a,b,d)0||cross(c,d,a)*cross(c,d,b)>0
	  bool intersect(point a,point b,point c,point d){//线段ab和cd是否相交
	      if(cross(a,b,c)*cross(a,b,d)>0) return 0;
	      if(cross(c,d,a)*cross(c,d,b)>0) return 0;
	      return 1;
	  }
	  double dis(point a,point b){return sqrt((a.x-b.x)*(a.x-b.x)+(a.y-b.y)*(a.y-b.y));}//两点距离
	  point norm(point a){return a/len(a);}//单位向量
	  point Joint(point a,point b,point c,point d){//求出直线ab和直线cd的交点
	  	point u=b-a,v=d-c,w=a-c;
	  	double t=(v*w)/(u*v);
	  	if(fabs(u*v)=da&&R>=db) return a*b/2;//case2:ab在圆内
	      point pa,pb;
	      double d=JointPoint_line_with_circle(a,b,R,pa,pb);
	      if(R=da) return a*pb/2+sector(pb,b,R);//case4,5:a在圆内
	      if(R>=db) return sector(a,pa,R)+pa*b/2;//b在圆内
	      return sector(a,pa,R)+pa*pb/2+sector(pb,b,R);//ab是割线
	  }
	  point Circle_Center(point A,point B,point C){//三角形外接圆圆心
	    	if(fabs((B-A)*(C-A)) p){//多边形重心公司
	  	int siz=p.size();
	  	double A=0,a;
	  	p.push_back(p[0]);
	  	point g;
	  	for(int i=0;i p){
	  	int siz=p.size();
	  	if(siz p){//判断点u在多边形(内:1,外:0,上:-1)
	  	point v(1e9+5,2e8+5);
	  	int cnt=0,siz=p.size();
	  	p.push_back(p[0]);
	  	for(int i=0;i1&&cross(s[top-1],s[top],p[i])=1;i--){//上凸包
	  		while(top>t&&cross(s[top-1],s[top],p[i])cross(s[i],s[j],s[a+1])) a=a%n+1;
	              while(cross(s[i],s[j],s[b])eps?A
	      int k=0; double res=0;
	      for(int i=h;i>n;
	      for(int i=1;i>m;
	          for(int j=1;j>p[j].x>>p[j].y;
	          for(int j=1;j midperp(point a,point b){return {(a+b)/2,point(b.y-a.y,a.x-b.x)};}//中垂线
	  circle cover(point a,point b){return{(a+b)/2,dis(a,b)/2};}//以a,b为直径的圆
	  circle cover(point a,point b,point c){//过三点的圆
	  	auto u=midperp(a,b), v=midperp(a,c);
	  	auto p=getNode(u.first,u.second,v.first,v.second);
	  	return {p,dis(p,a)};
	  }
	  
	  void increment(int n){//最小圆覆盖（随机增量法）
	  	random_shuffle(p+1,p+n+1);
	  	cir={p[1],0};
	  	for(int i=2;i>n;
	  	for(int i=1;i>p[i].x>>p[i].y;
	  	increment(n);
	  	printf("%.10lf\n%.10lf %.10lf\n",cir.r,cir.p.x,cir.p.y);
	  }
	  //-----------------------------------------------------------
	  point aa[N],bb[N];
	  double divide(int l,int r){//最近平面点对:分治+归并排序 O(NlogN)
	  	double d=2e9;
	  	if(l==r) return d;
	  	int m=l+r>>1; point tmp=aa[m];
	  	d=min(divide(l,m),divide(m+1,r));
	  	int i=l,j=m+1,k=0,t=0;
	  	while(i>n;//n>aa[i].x>>aa[i].y;
	  	auto cmp=[](point a,point b){return a.x p){//多边形重心公司
	  	int siz=p.size();
	  	double A=0,a;
	  	p.push_back(p[0]);
	  	point g;
	  	for(int i=0;i p){
	  	int siz=p.size();
	  	if(siz p){//判断点u在多边形(内:1,外:0,上:-1)
	  	point v(1e9+5,2e8+5);
	  	int cnt=0,siz=p.size();
	  	p.push_back(p[0]);
	  	for(int i=0;i=0&&i=0&&j-2) turn(i-1,j,x0-z,y,x-x0,x0-H,y0,H,W,L);
	  		if(j-2) turn(i,j-1,x,y0-z,y-y0,x0,y0-H,L,H,W);
	  	}
	  }
	  //(x1,y1,z1), (x2,y2,z2): 两个点的坐标
	  //L,W,H: 长方体的长、宽、高
	  int cal(int x1,int y1,int z1,int x2,int y2,int z2,int L,int W,int H){
	  	if(z1!=0&&z1!=H)if(y1==0||y1==W)
	  		swap(y1,z1),swap(y2,z2),swap(W,H);
	  	else
	  		swap(x1,z1),swap(x2,z2),swap(L,H);
	  	if(z1==H) z1=0,z2=H-z2;
	  	r=~0U>>1;
	  	turn(0,0,x2-x1,y2-y1,z2,-x1,-y1,L,W,H);
	  	return r;
	  }
	  void solve(){
	  	int L=10,W=8,H=6;
	  	int dis=sqrt(cal(2,3,0, 7,5,6,L,W,H));
	  	cout
		  using namespace std;
		  #define int long long
		  #define endl '\n'
		  
		  #define double long double
		  const double eps=1e-18;
		  const double PI=acos(-1.0);
		  const int N=4e5+5;
		  const int INF=0x3f3f3f3f3f3f3f3f;
		  int sgn(double x){return xeps;}//-1,0,1
		  
		  //-----------------------------------------------------------
		  struct point{
		      double x,y;
		      point(double x,double y):x(x),y(y){}
		      point():x(0),y(0){}
		  	point operator+(point b)const{return point(x+b.x,y+b.y);}
		      point operator-(point b)const{return point(x-b.x,y-b.y);}
		      point operator*(double t)const{return point(x*t,y*t);}
		      point operator/(double t)const{return point(x/t,y/t);}
		      double operator*(point b)const{return x*b.y-y*b.x;}//叉积
		      double operator&(point b)const{return x*b.x+y*b.y;}//点积
		  	double len()const{return sqrt(x*x+y*y);}
		      point norm()const{return*this/len();}
		  	bool operator==(const point &b)const{return sgn(x-b.x)==0&&sgn(y-b.y)==0;}
		  	bool operator!=(const point &b)const{return sgn(x-b.x)||sgn(y-b.y);}
		  	int Quadrant(){//获得象限(1,2,3,4)
		  		if(sgn(x)>0&&sgn(y)>=0) return 1;
		  		if(sgn(x)0) return 2;
		  		if(sgn(x)0
		  //直线ab与线段cd有交点: cross(a,b,c)*cross(a,b,d)0||cross(c,d,a)*cross(c,d,b)>0
		  bool intersect(point a,point b,point c,point d){//线段ab和cd是否相交
		      if(cross(a,b,c)*cross(a,b,d)>0) return 0;
		      if(cross(c,d,a)*cross(c,d,b)>0) return 0;
		      return 1;
		  }
		  double dis(point a,point b){return (a-b).len();}//两点距离
		  point norm(point a){return a/a.len();}//单位向量
		  point Joint(point a,point b,point c,point d){//求出直线ab和直线cd的交点
		  	point u=b-a,v=d-c,w=a-c;
		  	double t=(v*w)/(u*v);
		  	if(fabs(u*v)=da&&R>=db) return a*b/2;//case2:ab在圆内
		      point pa,pb;
		      double d=JointPoint_line_with_circle(a,b,R,pa,pb);
		      if(R=da) return a*pb/2+sector(pb,b,R);//case4,5:a在圆内
		      if(R>=db) return sector(a,pa,R)+pa*b/2;//b在圆内
		      return sector(a,pa,R)+pa*pb/2+sector(pb,b,R);//ab是割线
		  }
		  point Circle_Center(point A,point B,point C){//三角形外接圆圆心
		    	if(fabs((B-A)*(C-A))0) return v3.len();
		  		return fabs((v1*v2)/v1.len());
		  	}
		  	bool dot_on_seg(point p){return onSegment(p,s,e);}
		  	bool dot_on_line(point p){return fabs((e-p)*(s-p))eps?A{
		  	poly()=default;
		  	poly(vector& v):vector(v){}
		  	//poly(initializer_list init) : vector(init) {}
		      // 多边形面积-鞋带公式
		      double area(){
		          int n=size();
		          if(n0;
		          bool s[3]={0,0,0};
		          for(int i=0,j=n-1,k=n-2;i=2&&cross(s[s.size()-2],s.back(),p)=0;--i){
		              auto &p=a[i];
		              while(s.size()>t&&cross(s[s.size()-2],s.back(),p)1) s.pop_back();// 移除重复起点
		          return s;
		      }
		      //判断点和多边形的位置关系(射线法)__O(N)
		      int In_Poly(point P){
		          int n=size();
		          if(n==0) return 0;
		          if(n==1) return at(0)==P?-1:0;
		          if(n==2) return onSegment(P,at(0),at(1))?-1:0;
		          bool flag=false;
		          for(int i=0,j=n-1;i0==sgn(P.y-p2.y)>0) continue;
		              // 使用你的intersect函数判断射线与边相交
		              point ray_end(P.x+1e9,P.y);// 向右的射线
		              if(intersect(P,ray_end,p1,p2)) flag=!flag;
		          }
		          return flag;
		      }
		  	int In_poly(point u){
		          point v(1e9+5,2e8+5); // 射线终点
		          int cnt=0;
		          int n=size();
		          if(n==0) return 0;
		          push_back(at(0));
		          for(int i=0;i=0;});
		          if(it==begin()+1||it==end()) return -1;
		          point A=*prev(it),B=*it;
		          double crs=cross(A,B,P);// (A-P)×(B-P)
		          return sgn(crs);
		      }
		  	// 多边形重心计算
		      point centroid(){
		          int n=size();
		          if(n==0) return point();
		          double A=0,a;
		          point g;
		          push_back(at(0));
		          for(int i=0;iAndrew();// 先求凸包
		          int n=hull.size();
		          if(n
		                    cross(hull[i],hull[(i+1)%n],hull[j]))
		                  j=(j+1)%n;
		              ans=max(ans,dis(hull[i],hull[j]));
		          }
		          return ans;
		      }
		      //最小外接矩形面积（旋转卡壳）
		      double min_bounding_rectangle(){
		          poly hull=this->Andrew();
		          int n=hull.size();
		          if(ncross(a,b,hull[k])) k=(k+1)%n;
		              // 找到在ab垂直方向上投影最远的点
		              point perp((a-b).y,(b-a).x);// 垂直向量
		              while((perp&(hull[(j+1)%n]-a))>(perp&(hull[j]-a))) j=(j+1)%n;
		              double hig=cross(a,b,hull[k])/(b-a).len();
		              double wid=(perp&(hull[j]-a))/perp.len();
		              ans=min(ans,hig*wid);
		          }
		          return ans;
		      }
		  };
		  void test_poly_functions(){
		  	vector t1={ {0,0},{4,0},{4,3},{0,3} };
		      poly square=t1;// 创建多边形
		      cout  t2={ {0,0},{1,2},{2,1},{1,0.5} };
		      poly points=t2;
		      poly convex_hull = points.Andrew(); // 凸包
		      cout 1&&cross(s[top-1],s[top],p[i])=1;i--){//上凸包
		  		while(top>t&&cross(s[top-1],s[top],p[i])cross(s[i],s[j],s[a+1])) a=a%n+1;
		              while(cross(s[i],s[j],s[b])
		      int k=0;double res=0;
		      for(int i=h;i>n;
		      for(int i=1;i>m;
		          for(int j=1;j>p[j].x>>p[j].y;
		          for(int j=1;j midperp(point a,point b){return {(a+b)/2,point(b.y-a.y,a.x-b.x)};}//中垂线
		  circle cover(point a,point b){return{(a+b)/2,dis(a,b)/2};}//以a,b为直径的圆
		  circle cover(point a,point b,point c){//过三点的圆
		  	auto u=midperp(a,b),v=midperp(a,c);
		  	auto p=getNode(u.first,u.second,v.first,v.second);
		  	return {p,dis(p,a)};
		  }
		  //最小圆覆盖（随机增量法）
		  void increment(int n){
		  	random_shuffle(p+1,p+n+1);
		  	cir={p[1],0};
		  	for(int i=2;i>n;
		  	for(int i=1;i>p[i].x>>p[i].y;
		  	increment(n);
		  	printf("%.10lf\n%.10lf %.10lf\n",cir.r,cir.p.x,cir.p.y);
		  }
		  //-----------------------------------------------------------
		  //最近平面点对:分治+归并排序 O(NlogN)
		  point aa[N],bb[N];
		  double divide(int l,int r){
		  	double d=2e9;
		  	if(l==r) return d;
		  	int m=l+r>>1;point tmp=aa[m];
		  	d=min(divide(l,m),divide(m+1,r));
		  	int i=l,j=m+1,k=0,t=0;
		  	while(i>n;//n>aa[i].x>>aa[i].y;
		  	auto cmp=[](point a,point b){return a.x=0&&i=0&&j-2) turn(i-1,j,x0-z,y,x-x0,x0-H,y0,H,W,L);
		  		if(j-2) turn(i,j-1,x,y0-z,y-y0,x0,y0-H,L,H,W);
		  	}
		  }
		  //(x1,y1,z1),(x2,y2,z2): 两个点的坐标
		  //L,W,H: 长方体的长、宽、高
		  int cal(int x1,int y1,int z1,int x2,int y2,int z2,int L,int W,int H){
		  	if(z1!=0&&z1!=H)if(y1==0||y1==W)
		  		swap(y1,z1),swap(y2,z2),swap(W,H);
		  	else
		  		swap(x1,z1),swap(x2,z2),swap(L,H);
		  	if(z1==H) z1=0,z2=H-z2;
		  	r=~0U>>1;
		  	turn(0,0,x2-x1,y2-y1,z2,-x1,-y1,L,W,H);
		  	return r;
		  }
		  void Distance_between_two_points_on_the_surface_of_a_rectangular_prism(){
		  	int L=10,W=8,H=6;
		  	int dis=sqrt(cal(2,3,0,7,5,6,L,W,H));
		  	cout<<dis<<endl;
		  }
		  //-----------------------------------------------------------
		  //球面上两点之间的最短路径距离距离
		  double dis(double lati1,double longi1,double lati2,double longi2,double R){
		  	double pi=acos(-1.0);lati1*=pi/180,longi1*=pi/180,lati2*=pi/180,longi2*=pi/180;//角度转弧度
		  	//球坐标到直角坐标的转换公式：
		  	//x=cos(纬度)*sin(经度)
		  	//y=cos(纬度)*cos(经度)
		  	//z=sin(纬度)
		  	double x1=cos(lati1)*sin(longi1),y1=cos(lati1)*cos(longi1),z1=sin(lati1);
		  	double x2=cos(lati2)*sin(longi2),y2=cos(lati2)*cos(longi2),z2=sin(lati2);
		  	//计算球心角theta,返回球面距离
		  	double theta=acos(x1*x2+y1*y2+z1*z2);return R*theta;
		  }
		  //示例：计算北京和上海之间的距离
		  //北京: 39.9°N,116.4°E
		  //上海: 31.2°N,121.5°E’
		  //-----------------------------------------------------------
		  
		  signed main(){
		  	ios::sync_with_stdio(false);
		  	cin.tie(nullptr);
		  	//test_poly_functions();
		  	return 0;
		  }
		  ```
- 图片
  -   
- 文案- ✨ 模板特色:
  - ✅ 数据结构齐全：point, line, circle, poly（多边形）全都有。
  - ✅ 功能覆盖极广：
  - 从点线运算 → 圆与直线关系 → 凸包 / 旋转卡壳 → 半平面交 → 最近点对 → 最小圆覆盖。
  - ✅ 高精度 + 稳定性强：统一 long double 精度，内置 eps 容错。
  - ✅ 极易拓展：模块化结构，每个部分都能单独抽出使用。
  - ✅ OI 级性能：O(N log N) 凸包与最近点对算法，支持 1e5+ 数据规模。
  - 🧩 模块速览:
  - 📍 点与向量运算（struct point）
  - 加减 / 叉积 / 点积 / 长度 / 单位化
  - 求垂足、旋转、对称点、两点距离
  - 求直线交点、点到直线距离
  - 支持 Quadrant() 判断象限
  - 📏 直线与线段（struct line）
  - 点到线段距离
  - 判断点是否在线段上 / 直线上
  - 直线极角、两线是否平行
  - 🔺 多边形（struct poly）
  - 面积计算（鞋带公式）
  - 判断凸性 Is_Convex()
  - Andrew 凸包算法
  - 点与多边形关系（射线法 / 对数法）
  - 凸包直径 & 最小外接矩形（旋转卡壳）
  - 重心计算
  - 整点计数（皮克定理）
  - 🌙 圆与几何关系
  - 圆与直线交点
  - 扇形面积 / 圆弧面积计算
  - 外接圆圆心 Circle_Center()
  - 三点确定圆
  - 最小圆覆盖（随机增量法）
  - ⚙️ 高级算法
  - 半平面交（求多个多边形公共区域）
  - 最近点对（分治 + 归并 O(N log N)）
  - 自适应 Simpson 积分
  - 长方体表面两点最短距离
  - 球面两点最短弧线距离（经纬度计算）
  - 🧭 适用场景:
  - 🏗️ 计算几何专题 / 比赛模板库
  - 📚 数学建模 / 工程计算
  - 🧩 地图导航、最短路投影、形状拟合
  - 🧠 学习几何算法、可视化研究
  - 🛠️ 优化建议:
  - 若仅需单模块（如凸包），可独立抽取 poly 与 cross() 函数。
  - 若要加速浮点运算，可改为 double 并放宽 eps。
  - 对应精度问题，可配合 std::setprecision(12) 打印结果。