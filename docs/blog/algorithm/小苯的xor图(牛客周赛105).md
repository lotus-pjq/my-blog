---
title: 小苯的xor图(牛客周赛105)
date: 2025-08-17
category: 题解
tags:
  - 算法
outline: deep
---

- 题目：
- Code
```C++
#include
using namespace std;
#define int long long
#define endl '\n'
#define rep(i,j,k) for (int i=j;i g[N];
int n,m;
int a[N],dp[64][2];
signed main(){
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  cin>>n>>m;
  rep(i,1,n) cin>>a[i];
  int x,y;
  rep(i,1,m){
    cin>>x>>y;
    g[x].pb(y);g[y].pb(x);
  }
  int ans=0;
  for(int u=1;u>p&1) cur=(cur+(1>p&1) dp[p][1]++;
        else dp[p][0]++;
      }
    }
    ans=(ans+cur)%mod;
  }
  cout<<ans;
  return 0;
}
```
```
```