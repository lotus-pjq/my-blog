---
title: 杭电多校8
date: 2025-08-11
category: 其他
tags:
  - 算法
outline: deep
---

### D.最糖的题目
- 
- **shift 操作一次“整段左移一位”等价于一次“相邻元素交换 k-1 次”。**
	  无论你把操作看成“对子串”还是“对子序列”做，生成的置换群完全一样，所以判定条件不变。
	  ----------------
	  元素多重集合检查：首先检查两个序列的元素组成是否相同（排序后比较）
	  特殊情况处理：
  - k=1：序列必须完全一致
  - n=k：两个序列必须循环同构
  - n>k的情况：
  - k为偶数：总是可行
  - k为奇数：
    - 序列有重复元素：总是可行
    - 元素全不相同：检查从a到b的置换是否为偶置换
- ```C++
	  #include
	  using namespace std;
	  #define INF 0x3f3f3f3f3f3f3f3f
	  #define rep(i,j,k) for (int i=j;i=k;--i)
	  #define int long long
	  #define endl '\n'
	  //最小表示法
	  int GetMin(vector s,int n){
	  	int i=0,j=1,k=0;
	      while(i0) i+=k+1;
	              else j+=k+1;
	              if(i==j)j++;
	              k=0;
	          }
	      }
	      return min(i,j);
	  }
	  vector Minexp(vector s) {
	      int n=s.size();
	      if(n res;
	      for(int p=0;p a;          // 下标 0..n-1 对应原数组 1..n
	  	#define lowbit(x) ((x)&-(x))
	      Fenwick(int n_=0){init(n_);}
	      void init(int n_){n=n_;a.assign(n,0);}
	      void add(int x,int v){for(int i=x+1;i0;i-=i&-i)
	              ans+=a[i-1];
	          return ans;
	      }
	      int rangeSum(int l,int r){return sum(r)-sum(l-1);}
	  //select(k):返回最大的下标x,使得前缀和a[1]+...+a[x]≤k。(下标从0开始,找不到时返回0)
	      int select(int k){
	          int x=0;
	          for(int i=1>=1)
	              if(x+i& a) {
	      int n=a.size();
	      if(n==0) return 0;
	      vector tmp = a;// 离散化
	      sort(tmp.begin(), tmp.end());
	      tmp.erase(unique(tmp.begin(), tmp.end()), tmp.end());
	      int m = tmp.size();
	      Fenwick bit(m); 
	      long long res=0;
	      for (int i=n-1;i>=0;--i){
	          int id=lower_bound(tmp.begin(),tmp.end(),a[i])-tmp.begin()+1; 
	          res+=bit.sum(id - 1);  
	          bit.add(id, 1); 
	      }
	      return res;
	  }
	  void solve(){
	  	int n,k;cin>>n>>k;
	  	vector a(n),b(n);
	  	rep(i,0,n-1) cin>>a[i];
	  	rep(i,0,n-1) cin>>b[i];
	  	vector c=a;
	  	vector d=b;
	  	sort(c.begin(),c.end());
	  	sort(d.begin(),d.end());
	  	if(c!=d){cout amin=Minexp(a);
	  		vector bmin=Minexp(b);
	  		if(amin==bmin) cout>T;
	      while (T--) solve();
	      return 0;
	  }
	  ```
- ### 12.最多变的序列 #计数 #DP
- 洛谷原题：[P10741 [SEERC 2020] Fence Job - 洛谷](https://www.luogu.com.cn/problem/P10741)
- 
- #### 观察：
  - 首先不难发现，假设某一段数字 *a**l*​∼*a**r*​ 最后全变成了 *x*，那么 *x* 的位置 *p**o**s**x*​ 一定满足：
  - *l*≤*p**o**s**x*​≤*r*。
  - ∀*l*≤*i*≤*r*,*a**i*​≥*x*。
  - 那么，我们就可以 *O*(*n*2) 求出每个数最大能覆盖的区间 *a**l*​∼*a**r*​。
		  并且不难发现，我们最后的答案一定是由这些连续段拼接起来的。
- #### dp 设计：
  - 因为有了这个连续段性质，所以我们就比较容易 dp 了。
		  一开始用的是 *d**p**i*,*j*​ 表示考虑到第 *i* 位，第 *i* 位是原数列里第 *j* 个数的方案数。这种状态定义下利用前缀和优化 dp 其实是可以 *O*(*n*2) 得到答案的，但是转移方程细节有点多，这里不再赘述。
		  但是实际上这题有更简单的一个状态定义，同样是 *d**p**i*,*j*​，但是表示的却是考虑到第 *j* 位，这 *j* 个位的数字所处的**下标**全都在 1∼*i* 之间的方案数是多少（这里的 *i*,*j* 和前一个状态定义颠倒了一下，但是无伤大雅）。
		  这两种 dp 的状态定义的唯一区别就在于一个是限制了这一位必须是 *j*，一个限制了这一位只要小于等于 *j* 即可。这种通过==**把固定某个元素转化为固定某个前缀**的状态优化==是一种常见的优化转移方式，无论是计数 dp 还是最优化 dp 都可以用到，并且还方便统计答案。
		  那么预处理每个数能够拓展到的左右端点后直接转移即可：
  - 不妨定义 ==*d**p*[*i*][*j*] ==表示==使用前 *i* 个数字，填前 *j* 个的位置的方案数==。
  - 那么可以先求出第 *i* 个数字的拓展区间，即两边比其大的数都可拓展。
  - 那么：
  - 如果 *j* 在 *i* 的填充区间之外，那么考虑继承 即 *d**p*[*i*][*j*]=*d**p*[*i*−1][*j*]。
  - 如果 *j* 在 *i* 的填充区间之内，那么考虑继承+转移， 即 *d**p*[*i*][*j*]=*d**p*[*i*−1][*j*]+*d**p*[*i*][*j*−1]。
  - 发现状态转移方程只与上一层有关，那么就可以用滚动数组优化即可。
  - 时间复杂度：O(n^2)。
- 没压缩空间
	  ```C++
	  #include
	  using namespace std;
	  #define int long long
	  #define endl '\n'
	  #define rep(i,j,k) for (int i=j;i>n;
	  	rep(i,1,n) cin>>h[i];
	  	rep(i,1,n) dp[i][0]=1;
	  	rep(i,1,n){
	  		int l=i,r=i;
	  		while(l-1>=1&&h[i-1]>=h[i]) l--;
	  		while(r+1=h[i]) r++;
	  		rep(j,1,n){
	  			dp[i][j]=dp[i-1][j];
	  			if(l
	  using namespace std;
	  #define int long long
	  #define endl '\n'
	  #define clr(f,n) memset(f,0,sizeof(int)*(n))
	  #define cpy(f,g,n) memcpy(f,g,sizeof(int)*(n))
	  #define INF 0x3f3f3f3f3f3f3f3f
	  #define rep(i,j,k) for (int i=j;i=k;--i)
	  //#define mid (l+r>>1)
	  //#define ls x>n;
	  	rep(i,1,n) cin>>h[i];
	  	dp[0]=1;
	  	rep(i,1,n){
	  		int l=i,r=i;
	  		while(l-1>=1&&h[i-1]>=h[i]) l--;
	  		while(r+1=h[i]) r++;
	  		rep(j,1,r) dp[j]=(dp[j]+dp[j-1])%mod;
	  	}
	  	cout<<dp[n];
	  	return 0;
	  }
	  ```