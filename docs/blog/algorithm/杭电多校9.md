---
title: 杭电多校9
date: 2025-08-15
category: 其他
tags:
  - 算法
outline: deep
---

## 1009乘法逆元- #异或
- 题目：
	  
  - 代码：
		  ```C++
		  #include
		  using namespace std;
		  #define int long long
		  #define endl '\n'
		  const int mod=998244353;
		  const int N=2e5+5;
		  int h,n,m,a[N];
		  __int128_t ans,p;
		  __int128_t ksm(int x,__int128_t y,int mod=998244353){__int128_t ans=1;while(y){if(y&1)ans=ans*x%mod;x=x*x%mod;y>>=1;}return ans;}
		  __int128_t ksm2(int x,__int128_t y){__int128_t ans=1;while(y){if(y&1)ans=ans*x;x=x*x;y>>=1;}return ans;}
		  void solve(){
		  	cin>>h;p=h;
		  	int k=(p-1)/119+1;
		  	ans=0;
		  	if(p>t;
		  	while(t--) solve();
		  	return 0;
		  }
		  ```
- ## 1005计算几何- 题目：
	   
- ```C++
	  #include
	  using namespace std;
	  #define rep(i,j,k) for (int i=j;i>n;int i;
	      for(i=1;i>x[i]>>y[i];
	      }int mu=1;
	      for(i=2;i0) u[++num]={p[1].x,p[1].y,1};
	  	else if(cal0) u[++num]={p[i].x,p[i].y,1};
	  		else u[++num]={p[i].x,p[i].y,-1};
	  	}
	  
	  	cal=(p[1]-p[z])*(p[z]-p[z-1]);
	  	if(cal>0) u[++num]={p[z].x,p[z].y,1};
	  	else if(cal>t;
	  	while(t--) solve();
	  	return 0;
	  }
	  ```
-